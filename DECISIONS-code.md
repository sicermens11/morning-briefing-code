# 설계 결정 이력 — Claude Code판

Cowork판의 `DECISIONS.md`(138차까지 누적)와 같은 역할. **코드판에서 내린 결정과 그 배경**을 기록한다.

## 왜 남기나

Cowork 이력이 이식 작업에서 결정적으로 도움이 됐다. "왜 이 규칙이 이렇게 돼 있나"를 되짚을 수 있어서,
바꿔도 되는 것과 건드리면 안 되는 것을 구분할 수 있었다. 실제로 이 파일이 없었으면:
- 요일 분산이 **MCP 예산 제약** 때문이라는 걸 몰라 그대로 뒀을 것이다(129차 기록에서 발견)
- 해외 개별종목 시세가 **FMP 페이월** 때문에 기각된 걸 몰랐을 것이다
- OHLCV 100일 상한이 **API 1회 최대치**였다는 걸 몰랐을 것이다

## 기록 원칙

- **결정보다 배경을 남긴다.** "무엇을 바꿨나"보다 "왜 그렇게 판단했나"가 나중에 쓸모 있다.
- **기각·보류도 남긴다.** 나중에 제약이 풀렸을 때 되살릴 수 있다. Cowork 이력에서 가장 값어치 있던 게 이 부분이었다.
- **실측 근거를 숫자로 남긴다.** "낡은 데이터가 왔다"가 아니라 "08-18자였고 직접 호출은 당일 16:07 기준이었다".
- **틀린 판단도 지우지 않고 정정으로 남긴다.** 같은 실수를 반복하지 않으려면 무엇이 틀렸는지가 남아야 한다.

---

## 2026-08-26 (23차) — 검증이 과한가 진단 + 충돌 3건 정리

사용자 질문: *"지금 브리핑의 검증이 충분해? 과해? 서로 충돌하거나 꼬이는 데는?"*

### 진단: 과하다

| | 이식 직후 | 지금 |
|---|---|---|
| SKILL 크기 | 61,000자 | **120,634자 (+98%)** ≈ 55,000토큰 |
| 점수 반영 항목 언급 | — | 18곳 |
| **"점수·등급 미반영" 명시** | — | **17곳** |

**SKILL이 매 턴 컨텍스트에 들어간다.** 어제 68턴 × 55K ≈ **3.7M** — 캐시읽기 8M의 **절반 가까이**다.

⚠️ **오늘 하루에 서술 전용 항목을 15개 추가했다** — 시장국면·후보간중복·신고가돌파·상대강도·
분기6개·외국인지분율추이·당좌비율·어닝서프라이즈·의견추이·등급변경·선물원자재·해외벤치마크·
지지저항·거래상태·재무3개년. **점수 반영은 거의 그대로인데 읽을거리만 두 배**가 됐다.

**그리고 사용자가 읽을 시간은 그대로다**(출근 전·점심). 메일 본문 상한은 2,000자 그대로인데
넣을 게 두 배다. **서술 전용이 늘면 출력 토큰이 늘고, 출력 토큰은 곧 시간이다**(약 70토큰/초).

⚠️ 그 15개 중 **유용하다고 검증된 것은 하나도 없다.**

### 충돌 3건 — "어느 쪽이 이 시스템의 신호인가"로 풀었다

**① 갭④(무반응 +1) ↔ 신고가돌파 — 방향이 정반대였다**

갭④는 *"안 움직여야 좋다"*, 신고가는 통념상 *"뚫으면 좋다"*. 규칙에 어느 쪽이 이 시스템의
신호인지 안 적혀 있었다.

→ **이 브리핑은 "재료는 나왔는데 아직 안 붙은 틈"을 찾는다.** 신고가 돌파는 **이미 붙는 중**이라는
뜻이므로 **좋은 신호가 아니라 경고**다. `신고가돌파`를 **"갭 전제 점검"**으로 재정의했다:
- **갭④와 양립 불가.** 둘이 동시에 나오면 **갭④ 판정이 틀린 것**이니 다시 확인한다.
- 갭①②가 있어도 "재료가 이미 반영되기 시작했을 수 있다"를 주의사항에 쓴다.
- ⚠️ 그렇다고 후보에서 빼지 않는다. 점수 미반영, 판단 재료로만 준다.

**② 52주고점 하락률 ↔ 신고가돌파 — 같은 축의 양끝을 따로 관리하고 있었다**

→ **하나의 축("52주 고점 대비 위치")으로 합쳤다.** −50% 이하는 등급조정④(유일한 점수 연결),
0%(돌파)는 갭 전제 점검, 그 사이는 서술.
⚠️ **"고점 대비 −40%"와 "신고가 돌파"를 같은 문단에서 각각 좋게 쓰지 않는다** — 동시 성립 불가다.

**③ 동종업계(당일) ↔ 상대강도(20/60/120일) — 같은 질문 두 번**

→ **역할을 갈랐다:**
| | 기간 | 질문 | 용도 |
|---|---|---|---|
| `동종업계` | 당일 | "오늘 **이 종목만** 오른 건가" | **갭 해석** |
| `_상대강도` | 20·60·120일 | "**그동안** 지수보다 어땠나" | **배경 서술** |

⚠️ 그리고 **`_상대강도`를 "강하다/약하다"로 읽지 말라**고 못 박았다. 이 시스템은 발굴 전 60일
수익률 중앙값이 **−32.7%**인 구조적 역추세라, **마이너스가 정상**이다.

### 덤 — 재무 소스가 다섯이었다

강화-F2 · VAL · RISK · `재무3개년` · `분기6개`. 같은 지표가 소스마다 값이 다르다(ROE 24.41 vs 20.2).
"출처 섞지 마라"고만 적어뒀는데 **소스가 다섯이면 섞일 수밖에 없다.**
→ **용도별 정본**을 표로 지정했다: 재무취약 판정은 **F2+RISK**(실패 시에만 네이버 폴백),
과거 밴드는 `재무3개년`, 분기 추세는 `분기6개`, 밸류에이션은 VAL.

### 지금 정리하지 않은 것 — 의도적

**서술 전용 15개를 지금 솎아내지 않았다.** 오늘 넣은 걸 오늘 빼는 셈이고 **근거가 없다.**
내일 브리핑에서 볼 것:
1. 본문이 **2,000자 안에** 들어갔나 (안 들어갔으면 서술 전용부터 잘렸을 것)
2. **읽는 데 몇 분** 걸리나 (09:30 전에 다 읽히나)
3. 갭④와 신고가돌파가 **같이 나왔나** (나왔으면 위 규칙이 작동했는지)
4. **실행 시간** — 28.4분에서 얼마나 늘었나

그 결과로 **뺄 것을 정한다.** 충돌 정리에 2,852자가 더 들었지만, **모순을 방치하면 내일
브리핑이 이상하게 쓴다**는 판단이었다.

---

## 2026-08-26 (22차) — 받아놓고 안 쓰던 데이터 3건 + 12:20 슬롯 + 기록·재생

### 받아오는데 안 쓰던 것 — 전수 조사

스크립트 8종의 출력 키를 스킬 전문과 대조했다. 대부분은 오탐(SKILL이 산문으로 언급)이었고,
**개념 단위로 다시 거르니 3건이 진짜 미사용**이었다.

**① 외국인 지분율 10일 추이** ⚠️ 가장 아까웠던 것
`수급10일` 응답에 **매일 오고 있었는데 규칙 어디에도 없었다.**
갭③은 **하루치 순매수**만 본다 — 그날 우연히 산 것과 **열흘째 꾸준히 늘리는 것**이 같은 +1점이다.
실측: 효성중공업 26.70 → 27.19(**+0.49%p, 증가일 8/9**) · LIG 26.66 → 27.24(**+0.58%p, 8/9**)
대비도 된다 — 성광벤드는 36.46 → 35.84(**−0.62%p, 증가일 3/9**)로 외국인이 빠지는 중이었다.
→ `외국인지분율추이` 필드로 계산해 넘기고, **갭③ 서술에 병기**하도록 SKILL에 명시. 점수 미반영.

**② 당좌비율** — 어제 `재무3개년`에 넣었는데 SKILL이 안 썼다.
100 미만이면 **1년 내 갚을 돈보다 당장 현금화할 자산이 적다**는 뜻이다.
실측 LIG: 63.60 → 61.56 → **49.98**(3년 연속 악화). 부채비율 446%와 겹쳐 보면 그림이 달라진다.
→ 주의사항에 쓰도록 명시.

**③ BPS·적극매수 세부·미국 섹터ETF 개별명** — 미사용이지만 **쓸 필요가 크지 않다**고 판단. 보류.

⚠️ 조사 중 버그 하나 발견: `_as_float`가 **`%` 기호를 안 벗겨** 지분율이 전부 **0.0**으로 나왔다.
   에러가 아니라 0이라 알아채기 어렵다. 벗기는 문자에 `%`·`+` 추가.

### 12:20 점심 체크 — 이미 다 준비돼 있었다

`entry-check` 스킬도 `check_entry.py`도 **1220 슬롯을 원래 지원**하고 있었다. **없던 건 예약작업뿐.**
S4U 등록이 관리자 권한을 요구해 `scripts\add-lunch-check.ps1`을 만들어뒀다(사용자가 1회 실행).

⚠️ **12:20은 09:05의 반복이 아니다.** 답하는 질문이 다르다:
| 슬롯 | 질문 | 쓰지 말 것 |
|---|---|---|
| 09:05 | **재료가 시초가에 이미 반영됐나** | 추세·수급·거래량(아직 노이즈) |
| 12:20 | **오전에 조건이 충족됐나, 흐름이 살아있나** | — |

**12:20의 핵심 질문은 "09:05에 보류였는데 오전에 조건을 넘겼나"**다. 그게 이 슬롯의 존재 이유다.

### Sonnet 전환 — 지금은 하지 않는다

확인해보니 **`entry-check`는 이미 Sonnet**이다(`run-skill.ps1` L123). 브리핑만 Opus다.

**지금 안 바꾸는 이유**: 비용이 이미 $58 → **$9.20(−84%)**로 내려왔고, 오늘 규칙을 대폭 바꿨는데
**전부 실전 미검증**이다. 여기서 모델까지 바꾸면 **품질 변화가 규칙 때문인지 모델 때문인지
구분할 수 없다.** 한 번에 한 변수만 바꾼다.
→ 표본이 쌓여 "등급이 작동하는가"가 확인된 뒤에 시도한다.

### 기록·재생 — 이제 쉬워졌다

AGENDA 4-2로 오래 미뤄뒀던 항목인데, **데이터 대부분이 로컬 스크립트로 옮겨와서 쉬워졌다.**

`run-py.ps1`이 수집 스크립트 7종의 응답을 `data\snapshots\<날짜>\`에 자동 저장한다.
⚠️ **모델이 아니라 셸이 파일로 쓴다** — 모델이 옮겨 적으면 그게 곧 시간이다(출력 토큰 ∝ 시간).
⚠️ 리포트·검산류는 제외한다 — 가공 결과라 재생에 쓸모없다.
⚠️ 같은 스크립트를 다른 인자로 여러 번 부르므로 **인자를 파일명에 녹여** 덮어쓰기를 막았다.
⚠️ 스냅샷 실패가 본 실행을 막지 않는다(부가 기능).

이제 **수집을 다시 돌리지 않고 STEP4~5(판정·서술)만 재실행**할 수 있다.
"PER·PBR 재점수화"처럼 *표본이 쌓인 뒤 재검토*로 미뤄둔 것들을 즉시 실험할 수 있게 됐다.

---

## 2026-08-26 (21차) — ❌ 표기가 세 가지를 뭉뚱그리고 있었다

사용자 지적: *"X가 기술적으로 안 된다는 건지, 의도적으로 반영을 안 했다는 건지,
논의 자체를 안 했다는 건지 모르겠다."*

맞다. PDF의 `❌`는 **전혀 다른 세 가지**를 한 기호로 표시하고 있었다:
① 기술적으로 불가 ② 의도적 배제 ③ **그냥 논의를 안 함**

그리고 사용자가 짚은 8개 중 **대부분이 ③**이었다 — 즉 **못 하는 게 아니라 안 해본 것**이었다.
실제로 두드려보니 절반 이상이 **이미 가진 데이터로 계산만 하면 되는 것**이었다.

### 되는 것 — 4건 즉시 반영

**#26 분기 실적 추세** (`fetch_stock.py` `분기6개`)
`finance/quarter` 엔드포인트가 **6개 분기**를 준다. 실측(효성중공업) 영업이익률
10.77 → 13.53 → 14.95 → 11.21 → 15.67 → **16.85**.
⚠️ **연간만 보면 최근 분기의 꺾임을 반년 뒤에야 안다.** "작년 좋았다"와 "지난 분기부터
나빠지는 중"은 전혀 다른 이야기인데 구분할 수단이 없었다.

**#8 신고가 돌파** (`compute_ta.py` `신고가돌파`)
지금까지 `week52_high_drawdown_pct`(고점 대비 **하락률**)만 봤다. **"얼마나 빠졌나"는 알아도
"고점을 뚫었나"는 몰랐다** — 둘은 정반대 국면인데 같은 숫자 하나로만 보고 있었다.
52주/6개월/3개월 순으로 돌파 여부를 낸다. 추가 조회 0회(시계열 이미 보유).

**#51 지수 대비 상대강도** (`compute_ta.py` `_상대강도`)
20·60·120일 코스피 대비 초과수익률. 동종업계는 **당일**만 비교하니 "최근 한 달간 지수보다
강했나"를 못 봤다. 실측: 효성중공업 20일 +7.33 / 60일 −4.42 / 120일 −10.22%p.
⚠️ **점수 미반영** — 모멘텀은 백테스트에서 성과 차이를 못 만들었다.
⚠️ 이 시스템은 발굴 전 60일 수익률 중앙값이 **−32.7%**인 구조적 역추세다 —
   **상대강도가 크게 마이너스인 건 여기서는 정상**이고, 그것만으로 후보를 빼면 안 된다.

**#15 과거 PER·PBR 밴드** (지침만 추가)
`재무3개년`에 **연도별 PER·PBR이 이미 오고 있었다.** 쓰지 않았을 뿐이다.
지금까지 현재 PER의 **절대값**만 보고 "고평가"라 했는데, 그건 이 회사가 역사적으로 어느
수준인지를 무시한 판단이다. 효성중공업 13.02 → 16.46 → 31.94면 **3년 만에 2.5배**고,
같은 31.94라도 "원래 30대인 회사"와 뜻이 전혀 다르다.

### 안 되는 것 — 확인 후 사유 확정

- **#16 EV/EBITDA** — `시총`은 있으나 **EBITDA·순부채가 없다**. 계산 불가.
- **#75 섹터 자금유입** — ETF에서 NAV·거래량만 온다. **순자산총액·설정좌수 시계열이 없어**
  유출입을 계산할 수 없다.
- **#10 지수 편입/제외(MSCI 등)** — 데이터 소스 자체가 없다. 뉴스·공시 우연에 의존.
- **#32 실적 추정치 변화** — 미국은 20차 Yahoo 돌파로 해결(어닝 서프라이즈 이력).
  국내는 `추정PER`·`추정EPS` **현재값만** 있고 시계열이 없어 "오르는 중인가"는 못 본다. → 🟡

### 교훈

**`❌` 하나로 "못 함"과 "안 해봄"을 같이 표시하면, 안 해본 것이 못 하는 것처럼 굳는다.**
앞으로 커버리지 표기는 넷으로 나눈다: ✅ 반영 / 🟡 부분 / ➖ **의도적 제외** / ❌ **기술적 불가**.
"논의 안 함"은 ❌가 아니라 **별도 표기**하거나 즉시 검토한다.

---

## 2026-08-26 (20차) — 막혔던 것 재시도, 2건 돌파 + 실행 건강도 신설

사용자가 *"못 한 것·미룬 것 중에 지금 할 만한 건 지금 하자"*고 해서 다시 붙었다.

### ⚠️ "로그가 2건이라 미룬다"는 틀린 이유였다

브리핑 실행 건강도 추적을 **"로그가 2건뿐이라"** 미뤘는데, 사용자가 짚었다.
**스크립트는 표본이 2건이든 작동하고, 지금 만들어야 그때부터 쌓인다.** 미룰수록 손해였다.
게다가 더 나은 소스가 있었다 — run-logs보다 **JSONL의 `errors` 배열이 구조화돼 있다.**

### 돌파 ① Yahoo `quoteSummary` 401 — crumb 인증으로 뚫었다

지금까지 "401로 막혔다"고 세 번 보고했던 항목이다. **쿠키 출처가 문제였다** —
`fc.yahoo.com`은 404를 던지고, **`finance.yahoo.com/quote/<티커>/`를 먼저 열어야** 유효한
쿠키가 잡힌다. 그 뒤 `/v1/test/getcrumb`로 crumb을 받아 붙이면 통과한다.

`fetch_sec.py --analyst <티커>` 신설. 지금까지 **아예 못 보던 것 셋**이 열렸다:
- **의견 추이**(0m~-3m) + `_매수비율_변화` — 강화-G는 "현재 매수비율" **스냅샷**만 봤다.
  **좋아지는 중인지 나빠지는 중인지**를 처음으로 본다.
- **등급 변경 이력** — 어느 증권사가 언제 올렸나/내렸나(`action`이 `up`/`down`이면 실제 변경).
- **어닝 서프라이즈** — 실측(NVDA) **4분기 연속 +3.5~5.5%**.

→ PDF 100항목의 **#27·#31이 ❌ → ✅**, #32가 🟡로 올라갔다.
⚠️ 점수는 기존 강화-G 규칙 그대로다. **서술 보강용**이다.

### 돌파 ② 공매도 — 경로는 찾았으나 결국 실패

네이버 종목 페이지에서 **공매도현황 탭**(`short_trade.naver`)을 찾았고, 그 iframe이
`data.krx.co.kr/comm/srt/srtLoader/index.cmd?screenId=MDCSTAT300&isuCd=...`로 연결된다.
**헤더까지 확인했다** — 종목명·일자·공매도 수량·공매도 금액·거래량·순보유잔고수량·업틱룰적용.

⚠️ **그런데 데이터는 JS가 채운다.** 올바른 Referer·세션 쿠키를 갖춰 `getJsonData.cmd`를 쳤지만
`bld` 이름 4가지를 추측해 전부 400이었다. **추측으로는 못 맞춘다.**
→ **화면ID `MDCSTAT300`까지는 확인됐다.** 다음에 시도할 사람은 여기서 출발하면 된다.

### 신설 ③ 실행 건강도 (`check_health.py`)

**매일 로그에 `_실패항목`이 남는데 아무도 모아 보지 않았다.** 그래서 같은 실패가 며칠 반복돼도
몰랐다 — 임시파일 충돌이 **매일** 났는데 모델이 매번 새 이름으로 우회해 로그엔 1건처럼 보였고,
`list_drafts` 초과가 **이틀 연속** 검증을 무력화했다. **하루치만 보면 사소한데 겹쳐 보면 명백한** 종류다.

JSONL `errors` + run-logs를 읽어 **유형별·반복유형·비용/시간 추이**를 낸다. 주간 리뷰에 연결.
⚠️ **`반복유형`이 핵심**이다 — 하루 1건은 사고, 2일 이상은 구조 문제.
⚠️ 분류가 안 되면 **`기타`로 둔다.** 억지로 분류하면 없는 패턴이 생긴다.

### 감사에서 하나 더 — 오타가 조용히 통과하고 있었다

`track_unmapped.py`·`check_map_health.py`·`check_health.py`가 `--report`를 **기본 동작**으로
처리해서, `--reprot` 같은 오타를 쳐도 **리포트가 정상 출력됐다.** "먹혔다"고 착각하기 딱 좋다.
→ 세 스크립트에 **알 수 없는 인자 거부** 가드를 넣었다. 검증 완료.

### PDF 정정 (`매수검토항목-100-커버리지.pdf` 2판)

이후 검정·확인으로 바뀐 6곳을 고쳐 재발행:
- #7 모멘텀 ❌→🟡 (검정: 성과 차이 없음)
- #17 배당 🟡→➖ (별도 프로젝트에서 커버 — **의도적 제외**)
- #27 어닝 서프라이즈 ❌→✅ (Yahoo 돌파)
- #31 컨센서스 추이 ❌→✅ (Yahoo 돌파)
- #32 EPS revision ❌→🟡
- #58 오버행 ❌→➖ (**매수기회포착과 메커니즘 방향이 반대**)
- #82 거래대금 ❌→🟡 (플래그는 있으나 숫자 기준 없음 + 검정 결과 병기)
→ 집계 **✅60 · 🟡14 · ❌24**. `➖ 의도적 제외` 기호를 새로 도입했다.

---

## 2026-08-26 (19차) — 시장국면·후보간중복·실패원인분류 신설

### ⚠️ 먼저: 내가 코워크 이력을 답처럼 썼다

사용자가 두 번 지적했다 — *"이력은 참고하되 답이 아닐 수 있다"*.
그런데 나는 "모멘텀은 67·70·72·137차에서 네 번 기각됐다"를 **근거로 내밀었다.**
그 기각 사유는 `market_get_movers`의 **데이터 노이즈**(상한가 소형주·레버리지 ETF 범벅)였고,
지금은 일봉 250일을 직접 갖고 있어 **전제가 다르다.**
→ **이력은 "그때 왜 안 됐나"의 참고다. 지금도 안 된다는 증거가 아니다.**

### 검정: 내가 1·2순위로 꼽았던 것 둘 다 근거 없음

어제 "표본에 근거 없음"이라고 했는데, 그건 **검정을 안 했다는 뜻**이었다. 실제로 재봤다:

| 검정 | 결과 |
|---|---|
| **유동성**(1순위) | 거래대금 낮은 쪽(<214억) **+8.00%p** vs 높은 쪽 +5.18%p — **오히려 반대** |
| **모멘텀**(2순위) | 발굴 전 20일 하락 중 +6.79%p vs 상승 중 +6.15%p — 차이 없음 |

⚠️ 유동성 검정의 한계: **"주가가 갔나"만 보고 "그 가격에 체결됐나"는 못 본다.**
유동성 문제의 본질은 후자라 **"무관하다"가 아니라 "이 방법으로는 못 잰다"**가 정확하다.

부수 발견: 브리핑이 고르는 종목의 **발굴 전 60일 수익률 중앙값이 −32.7%**다.
갭 전략이 자연히 **역추세**를 고르고 있다.

### 신설 ① 시장국면 (`compute_ta.py` `_시장국면`)

백테스트에서 **유일하게 유의에 가까운 축**이었다:

| 발굴 시점 코스피 | n | 평균 | 플러스 | p |
|---|---|---|---|---|
| 20일선 **아래** | 19 | **+8.80%p** | 14/19 | **0.064** |
| 20일선 **위** | 3 | **−7.46%p** | **0/3** | 0.25 |

산점도 단조롭다 — 코스피 20일선 대비 −20%일 때 픽 +9~18%p, +5%일 때 −5~−9%p.
논리도 맞는다: **시장이 눌려 있을 때 "재료 있는데 안 움직임"은 진짜 미반영**이고,
과열인데 안 움직였다면 **안 움직일 이유가 있는 것**이다.

⚠️ **점수·등급에 반영하지 않는다.** "위" 표본이 **3건뿐**이다 — 이걸로 등급조정 규칙을 만들면
3건에 시스템을 맞추는 것이다. 지금은 ①시장 한눈에에 **한 줄 서술** + STEP8 JSONL `market_regime`
기록만 한다. 몇 주 뒤 `backtest_returns.py`로 검정해 점수 반영 여부를 정한다.
→ **서술은 지금 해도 위험이 0이고, 점수 반영만 표본이 필요하다.** 어제 이 구분을 안 하고
   "기록만 하자"로 뭉뚱그린 게 설명 부족이었다.

2026-08-26 실측: 코스피 6,808.21 / SMA20 6,499.60 → **+4.75% 과열 구간**.

### 신설 ② 후보간 중복 (`compute_ta.py` `_후보간중복`)

**후보를 여러 개 내면서 서로 얼마나 겹치는지는 한 번도 안 봤다.**
후보 둘이 다 전력인프라면 등급이 🟢·🟡라도 **한 종목에 두 번 베팅**하는 것이다.

최근 60거래일 일간수익률 상관. 실측:
- 효성중공업 ↔ 엘에스일렉트릭 **0.884**(같은 섹터 = 사실상 같은 베팅)
- 전력 ↔ 방산 ↔ 조선기자재 0.50~0.58(독립~부분중복)

⚠️ **추가 조회 0회** — `--codes` 모드가 이미 후보 전원의 250거래일을 받아왔다.
⚠️ **점수·등급 미반영, 무엇을 뺄지도 정하지 않는다.** "이 둘은 사실상 같은 베팅"이라고 알릴 뿐이다.

### 신설 ③ 실패 원인 분류 (주간 리뷰)

지금은 빗나간 픽에 **"−10.62%p"라는 숫자만** 남는다. **왜** 빗나갔는지가 없으니
"어떤 실패가 반복되는가"를 알 수 없다.

7개 태그로 분류: `재료소진`·`섹터역풍`·`개별악재`·`재무현실화`·`갭판정오류`·`시장국면`·`원인불명`.
전부 **이미 있는 데이터로 판별**된다(09:05 `gap_pct`, `동종업계`, `market_regime`).

⚠️ **`원인불명`을 둔 이유**: 억지로 분류하면 없는 패턴이 생긴다. **모르면 모른다고 적는다.**
⚠️ **하나만 고른다** — 두세 개 붙이면 집계가 안 된다.
⚠️ **사후 정보로 "그때 알 수 있었다"고 하지 않는다** — 발굴 시점에 알 수 있던 것만 근거로 쓴다.

4주 쌓이면 "규칙별 적중 현황"에 **가장 잦은 실패 유형**을 덧붙인다. 그게 다음 개선의 출발점이다.

### 사용자가 직접 하기로 한 것

**청산·매도 시점** — 시스템에 넣지 않는다. 사용자가 판단한다.

---

## 2026-08-26 (18차) — 주간 리뷰 이식 검증 + 주간 기능 2건 신설

### 이식 검증 결과: 유실 0건

코워크 원본 사본(`.\weekly-action-review\SKILL.md` 47.7KB)과 현행을 대조했다.
- **섹션**: 원본 21개 헤딩 중 현행에 없는 것 **0개**(STEP3은 오늘 제목만 바뀜)
- **규칙·수치**: 판정 ±1%p · 보합 처리 · 섹터 로테이션 7회 · 장기후보 45회 ·
  분기리뷰 90일/35회 · 자동감지 · 4주누적 · 등장회차 · 합산점수 — **전부 동일 개수로 보존**
- **MCP 도구 호출**: 원본에만 있는 것 **0개**

→ 이식은 충실했다. 코드판에서 늘어난 5개 헤딩은 전부 신설분(STEP 0 환경준비, 다지평 누적).

### 신설 ① 09:05 진입체크 판정 검증 ⚠️ 가장 큰 공백이었다

**`entry-check-log.jsonl`에 매일 판정이 쌓이는데 주간 리뷰가 그 파일을 한 번도 읽지 않았다**
(참조 0건 확인). *"진입가능"이라고 카톡을 보낸 픽이 실제로 잘 갔는지, "보류"가 옳았는지를
아무도 검증한 적이 없다.*

`backtest_returns.py --entry-check` 신설. **핵심질문 하나에 답한다** —
`진입가능`이 `보류`보다 실제로 잘 갔나. 차이가 플러스면 진입 조건이 작동한 것,
0 근처거나 마이너스면 조건이 무의미하거나 방향이 반대인 것.

⚠️ **10건 미만이면 본문에 싣지 않게** 했다. 지금은 D+1 매칭 4건뿐이다(진입체크가 08-25 시작).
⚠️ **사후 수익률로 그날 판정을 "틀렸다"고 하지 않는다** — 판정은 09:05 시점 정보로만 내린 것이다.

### 신설 ② 맵 건강검진 주간화

`check_map_health.py --stale-only`(약 1초)를 주간 리뷰에 넣었다.
`value-chain-map-updater`가 같은 검사를 하지만 **반기(4·9월)에만** 돈다 —
종목은 언제든 상장폐지되고, **죽은 종목 하나가 그 섹터 판단 전체를 망가뜨린다**
(`에이치디현대미포`가 8개월간 남아 있었다). 0건이면 아무것도 하지 않고, 1건 이상이면 한 줄 알린다.
**맵 수정은 하지 않는다** — 이 스킬의 일이 아니다.

### 검토했으나 지금은 안 한 것

- **브리핑 실행 건강도 추적**(run-logs에서 반복 실패·비용·소요시간 추이).
  가치는 있으나 **로그가 2건뿐**이고 08-25는 형식이 달라 파싱도 안 된다. 표본이 쌓인 뒤 재검토.

---

## 2026-08-26 (17차) — 백테스트 다지평 재계산, 내 결론 2건 철회

사용자가 *"27거래일치가 있으니 그걸 먼저 봐줘. 이력은 참고하되 이력이 답이 아닐 수 있다"*고 했다.
맞는 지적이었고 — **결과적으로 내 어제 결론 두 개가 무너졌다.**

### 먼저 판정 정의를 잘못 읽고 있었다

백테스트 표의 **🟡 "적중"은 주가가 부진했다는 뜻**이다(관망이 옳았음). 이걸 모르고 적중률과
수익률을 섞으면 안 된다. 갈라 보니:
- **매수권고 🔴🟢 15건: 적중 6 / 빗나감 9, 평균 −1.13%p**
- 관망 🟡 19건: 적중 11 / 놓침 8, 평균 −0.57%p
→ **관망이 매수권고보다 나았다.** 등급이 작동한다는 증거가 없다.

### 다지평 재계산 — 지평 하나만 보면 정반대 결론이 난다

일일 로그에서 발굴가가 있는 픽 36건(07-29~08-26)을 뽑아 D+1·D+5·D+20을 코스피 대비로 재계산했다.
**기준일을 먼저 검증했다** — 36건 중 32건이 직전 거래일 종가와 일치(개장 전 브리핑이므로 정상).

| 지평 | n | 초과(이론) | 초과(실전=발굴일 시가) |
|---|---|---|---|
| D+1 | 36 | −1.40%p | **−1.84%p** |
| D+5 | 28 | +5.52%p | **+4.84%p** |

**부호가 뒤집힌다.** 하루 뒤엔 지고 5거래일 뒤엔 이긴다 — 재료가 반영되는 데 시간이 걸린다는
뜻이고 갭 전략의 전제와 맞는다. 시초가 갭으로 잃는 몫은 작았다(평균 0.42%, D+5에서 −0.69%p).

### ⚠️ 철회 ① — "갭③이 반대로 작동한다"

어제 두 표본에서 같은 방향이 나왔다며 관찰로 올렸다. **기간을 통제하니 사라졌다:**

| | 초기(~08-05) | 후기(08-05~) |
|---|---|---|
| 갭③ 포함 | +12.87%p (n=2) | −4.93%p (n=5) |
| 갭③ 없음 | +18.34%p (n=9) | −3.54%p (n=6) |

**후기 구간에서는 −4.93 vs −3.54로 사실상 같다.** 갭③ 픽이 나쁜 기간에 몰려 있었을 뿐이다.
네 갭 모두 초기 +12~18%p / 후기 −5~−1%p로 **같이 움직였다 — 갭이 아니라 날짜가 결과를 갈랐다.**

⚠️ 그리고 **"두 번째 독립 표본"이라고 한 것도 틀렸다.** 같은 기간 데이터라 독립이 아니었다.

### ⚠️ 철회 ② — "실제로 판단을 바꾸는 공백 7개"

이력을 확인하니 두 개가 틀렸다:
- **가격 모멘텀**: 67차 기각 → 70차 재검토 → 72차 파일럿 → **137차 "기각 결론 여전히 유효, 재제안 없음"**.
  기각 사유는 철학이 아니라 **데이터 노이즈**(`market_get_movers`가 상한가 소형주·레버리지 ETF 범벅)였다.
- **배당**: 42차·71차 등 **세 번 배제**. 결정적으로 *"사용자의 별도 포트폴리오 프로젝트가 배당
  익스포저를 SCHD·TIGER미국배당다우존스 ETF로만 커버하기로 이미 정리"*돼 있었다.
  내가 "지금이 8~9월 배당시즌"이라 쓴 건 **71차 표현만 보고 배제 사유를 안 읽은 것**이다.

⚠️ 다만 사용자 지적대로 **기각 사유가 그때 도구의 한계였다면 지금은 다를 수 있다.**
모멘텀은 `market_get_movers` 노이즈가 문제였는데 지금은 일봉 250일을 직접 갖고 있다.
**이력은 "왜 그랬나"의 참고지 답이 아니다.**

### 유일하게 유의한 결과가 규칙이 아니라 시기였다

| 구간 | n | 평균 | 플러스 | p |
|---|---|---|---|---|
| 07-29~08-05 | 11 | **+17.34%p** | **11/11** | **0.001** |
| 08-05 이후 | 11 | −4.17%p | 3/11 | 0.227 |

코스피 자체는 +5.47% vs +2.80%로 차이가 2.7%p뿐인데 픽의 초과수익률 차이는 21%p다.
시장 방향이 아니라 **그 시기 픽들의 성격**이 달랐다는 뜻인데, 22건으로는 알 수 없다.

**결론: 지금 표본으로는 어떤 규칙도 검정할 수 없다. 필요한 건 새 데이터 항목이 아니라 시간이다.**

---

## 2026-08-26 (17차-b) — `backtest_returns.py` 신설 + 주간 리뷰 연결·수정

### 신설: 다지평 수익률 누적

`backtest_returns.py` — D+1·D+5·D+20 × **코스피 단일 벤치마크** × **실제 매수 가능가**(발굴일 시가).
`data\backtest-returns.jsonl`에 멱등 누적(D+20은 나중에 채워지므로 재실행이 전제).
기존 `backtest-track-record.md`는 **건드리지 않았다** — 섹터 ETF 기준이라 계열이 다르고 섞으면 둘 다 못 쓴다.

⚠️ **스크립트에 오늘 내가 밟은 함정의 방어장치를 넣었다**:
- `기간별`을 리포트에 항상 포함하고, **"갭별을 보기 전에 기간별을 먼저 보라"**를 출력에 박았다
- 평균만 보면 소수 종목에 끌려다니므로(상위 3건 빼니 +4.84 → +1.10%p) **중앙값·플러스 비율·p값**을 함께 낸다
- `기준일검증` 플래그 — 발굴가가 직전 거래일 종가와 맞는지. 4건이 걸러졌다
- `_주의`에 **"유의하지 않음은 차이가 없다가 아니라 아직 모른다는 뜻"**을 명시

### 주간 리뷰에서 고친 것

**① STEP3 한국 종목 조회 캡이 곧 터질 상태였다** ⚠️
`koreaStock-stock_get_price_history`가 **종목당 1회 MCP**에 "전체 최대 20회" 캡인데,
**후보 상한이 2→6개가 되면서 주당 최대 30픽이 가능해졌다.** 캡이 모자란다.
→ `backtest_returns.py --weekly`로 전환. **전 종목 1회, MCP 0회**(36종목 0.6초 실측). 캡 자체가 사라졌다.

**② 휴장일에 통째로 깨지던 문제**
기존 규칙은 시작·종료일이 **정확히 일치하는 행**만 썼다. 2026-08-17(광복절 대체휴일)을 시작일로
넣어보니 **8종목 전원 "데이터 미확인"**이 됐다.
→ 구간 안의 실제 거래일로 보정하되 **`보정됨: true`와 `실제사용일`을 함께** 돌려준다.
⚠️ 조용히 다른 날짜로 계산하지 않는다 — 보정 사실을 리뷰 본문에 적게 했다.

**③ 후보1~4 → 후보1~6** (15차 상한 변경의 누락분)

**④ "변경 전 원문" 블록이 틀린 내용을 담고 있었다**
*"발굴시점가는 intraday-monitor 전용이라 이 STEP과 무관"* — intraday-monitor는 2026-07-27에
삭제됐고, **지금은 발굴시점가가 `backtest_returns.py`의 핵심 입력**이다.
→ 원문은 남기되 **"그대로 따르지 말 것"** 경고를 붙였다.

### ⚠️ 정정 — 어제 PDF의 오류

`quarterly-position-review-history.md`를 *"파일은 있는데 어떤 스킬도 안 읽는다 — 끊어진 참조"*라고
썼는데 **틀렸다.** `weekly-action-review` STEP5가 **90일마다** 쓰는 파일이고 마지막 실행이 2026-08-04다.
내 확인 스크립트의 출력 버그를 그대로 믿었다.

---

## 2026-08-26 (16차) — 무료 데이터 소스 조사 및 4건 반영

지난번 FMP 페이월 기각이 **틀린 정보였던 것**(페이월은 FMP의 것이지 데이터의 것이 아니었다)을
겪었으므로, 이번엔 **후보를 전부 실제로 두드려보고 되는 것만** 채택했다.

### 반영 4건 — 전부 키 불필요, 알려진 값과 대조 검증 완료

**① SEC EDGAR Form 4 → `fetch_sec.py` 신설 (강화-B 전환, MCP −1회)**
- 기존 `AlphaVantage-INSIDER_TRANSACTIONS`는 **무료 티어 하루 25회 제한**이 있고,
  2026-08-21에 실제로 rate limit에 걸려 openinsider 스크래핑으로 폴백했다.
  **EDGAR는 그 데이터의 원천**이고 사실상 무제한이다.
- 대조 검증: 08-25 브리핑이 얻었던 *"Suzanne Nora Johnson 'A' 1,262+1,148주, 단가 0"*을 **정확히 재현**.
- ⚠️ **그리고 더 정확했다.** 브리핑은 *"Tench Coxe 'D' 500,000주 **처분** 병존"*이라고 썼는데
  EDGAR 원문은 **`G`(증여)**였다. AlphaVantage는 D(disposed)로만 줬고 EDGAR는 코드 G까지 준다.
  **증여를 매도로 읽으면 없는 악재를 만든다.** 거래코드 표를 SKILL에 넣었다.
- 티커→CIK 표(10,388건, 847KB)는 30일 캐시.

**② 네이버 재무제표 → `fetch_stock.py` `재무3개년` (강화-F2 교차검증·폴백)**
- 대조 검증: **부채비율 완전 일치**(효성중공업 190.31 · LIG 446.35). 순이익률은 8.43 vs 8.42(반올림).
  → 재무취약 판정의 두 조건(부채비율 200%+ / 순이익률 음수)은 어느 쪽을 써도 같은 결론이다.
- ⚠️ **MCP를 없애지 않고 폴백으로 뒀다.** F2에만 있는 CFO·FCF·이자보상배율·CAGR이 서술에 쓰인다.
  없애면 MCP는 아끼지만 **서술이 얇아진다** — 지금까지 없던 **폴백이 생긴 것**이 진짜 이득이다.
  (F2가 죽으면 예전엔 재무취약 판정 자체를 못 했다.)
- ⚠️ **ROE는 두 소스 값이 다르다**(24.41 vs 20.2 — 산출 기준 차이). 출처를 섞지 않도록 명시.
- ⚠️ 네이버가 주는 **마지막 열은 컨센서스 추정치**다(삼성전자 2026 영업이익률 53.05%). 스크립트가 잘라낸다.

**③ 거래정지·상장폐지 즉시 감지 (`fetch_stock.py` `거래상태`)**
- **상장폐지 종목은 `/basic`이 HTTP 409를 던진다.** 실측(`010620`)로 확인.
- 13차에서 만든 검출은 400일 시세를 받아 판별했는데, 이건 훨씬 싸고 후보 단계에서 바로 걸린다.

**④ 원/달러 → `fetch_us.py`에 `USDKRW=X` 추가 (ECOS 대체)**
- 사용자가 *"ECOS 대체재 없냐"*고 물어 찾았다. **답은 이미 쓰고 있는 야후**였다 —
  키도 새 의존성도 없고, 기준일 처리도 기존 로직 그대로다.
- ⚠️ **시장환율이지 매매기준율이 아니다**(1,385.38 vs 1,380.60). "매매기준율"이라 쓰지 않도록 명시.
  절대값보다 **매일 같은 출처를 써서 전일 대비 변화를 일관되게 보는 것**이 중요하다
  (2026-08-21에 한 브리핑 안에서 1,402.50/1,380.9/1,393.00 세 값이 갈렸다).

### 확인 실패 — 남은 공백

- **공매도 잔고** ⚠️ **가장 큰 공백.** KRX 공매도 포털이 JS 기반이라 직접 호출 불가,
  네이버에도 엔드포인트가 없다. 공매도 급증은 갭 판단에 중요한데 **지금 아예 못 본다.**
- **KRX 정보데이터시스템** — 전종목시세·투자자별·PER/PBR 전부 HTTP 400. 세션/OTP 절차 필요 추정.
- **Yahoo `quoteSummary`** — **401**(crumb 인증이 생겼다). 애널리스트 의견 추이·목표주가 변경 이력을
  못 쓴다. 지금 쓰는 `chart` 엔드포인트는 정상이다.
- 키 필요(무료): 한국은행 ECOS · FRED · 공공데이터포털 금융위 API.

### 만들면서 밟은 함정

`Accept-Encoding: gzip`을 헤더에 넣고 압축 해제를 안 했다 → 본문이 gzip 바이트로 와서
`JSONDecodeError`. urllib은 자동 해제를 하지 않는다. 헤더를 빼는 것으로 해결.

---

## 2026-08-26 (15차) — 후보 상한 4→6, 잘린 후보 기록 신설

사용자가 물었다 — *"후보 상한 4개가 타당한가? 같은 등급인데 상한 때문에 못 오르는 건 아닌가?"*

### 먼저 잰 것 — 상한은 지금까지 한 번도 걸린 적이 없다

일일 로그 35일치: **평균 1.77개**, 최대 4개(단 1일), 2개인 날이 19일(54%), 관망 3일.
그리고 **"개수 때문에 뺐다"는 기록이 어디에도 없다.** 제외 사유는 전부 실질적이었다 —
재무 부적격, 공시 해지, 갭 미성립, **맵 미등재**.

⚠️ **그런데 이 측정은 옛 상한에 잘려 있다.** 로그가 상한까지만 기록했으니 "몇 개가 있었는지"는
알 수 없다. **잘린 것을 기록한 적이 없어서 상한이 걸렸는지조차 확인할 수 없었다** —
8차의 미매핑과 **똑같은 패턴**이다.

### 정정 — "2군 = 3·4순위"는 내 오해였다

실제 기록을 보니 다르다:
> 대창솔루션 — *"순이익 적자·PBR 2.47·시총 792억 초소형주·거래정지 이력으로 **1군 미승격**"*
> 에스비비테크 — *"영업적자 미탈출 + 계약 금액 19억원 소규모로 **1군 미승격**"*

**2군은 순위가 아니라 "재료가 약해 보이니 검증 예산을 안 쓴다"는 품질 트리아지였다.**
검증이 싸진 지금 그 논리가 사라진 게 맞고, 폐지 결정 자체는 옳았다.
(08-12 로그에 `후보1~후보4`가 이미 있었다 — 옛 형식도 4개를 지원했다.)

### 진짜 문제는 남아 있다

**상한이 안 걸렸다는 건 과거 데이터일 뿐이고, 진짜 병목은 개수가 아니라 맵 게이트였다.**
오늘 그걸 풀었으니 **내일부터 공급이 늘 수 있다.** 과거가 내일을 예측하지 못한다.

그리고 구조가 그대로다 — **구체성(대리지표)으로 순위를 매겨 자르고, 그 다음 원점수(진짜 지표)를 잰다.**
대리지표가 결과와 얼마나 맞는지는 측정된 적이 없다. 사용자가 짚은 자리가 정확히 여기다.

### 반영

**① 검증 상한 4 → 6.** 비용 실측 기준이다:
후보 1개당 MCP = 강화-P 1 + F2/VAL/RISK 0~3 + BUYBACK 1 + PREF 1 = **최대 6회**(캐시 히트면 2~3).
6개면 합산 **32~48회 / 상한 70회**. 출력은 카드당 약 1.5K 토큰 × HTML 2회 = **개당 +43초**.
⚠️ **더 늘리려면 이 두 숫자를 다시 재고 올린다 — 감으로 올리지 않는다.**

**② `dropped` 필드 신설** — 상한에 걸려 못 본 후보를 종목코드와 함께 기록.
`append_log.validate()`가 **코드 누락 시 경고**한다(코드가 없으면 나중에 주가를 되짚을 수 없어
측정 자체가 불가능하므로).
⚠️ **갭 미충족으로 뺀 것은 넣지 않는다** — 섞이면 "상한이 문제인가"를 측정할 수 없다.
→ 몇 주 뒤 **"잘린 후보가 채택 후보보다 잘 갔나"**를 계산할 수 있게 된다.

**③ 검증 상한과 표시 상한을 분리했다.** 내가 둘을 섞어놨었다 —
검증은 **비용** 문제, 표시는 **사용자 주의력** 문제다. 검증은 넓게(6), 표시는 **등급으로** 자른다
(🔴/🟢 전체 카드 · 🟡 압축). entry-check 카카오는 **개수 고정을 없애고 픽 전부**로 바꿨다 —
개수를 박아두면 뒤쪽 픽이 조용히 빠지고 **그 종목은 아무도 확인하지 않는다.**

⚠️ **상한은 천장이지 목표가 아니다.** 35일 중 3일이 관망이었다. 숫자를 채우려고 갭 미충족
후보를 올리지 않는다.

### 감사에서 놓쳤던 것 1건 추가 발견

`append_log.validate()`가 `p.get("rank") in (1, 2)`로 **옛 rank 의미(1군)**를 쓰고 있었다.
그대로 두면 **rank 3~6 후보의 진입조건 누락이 조용히 넘어간다.** 전원 적용으로 수정.
→ 14차 전수조사에서도 이걸 못 잡았다. **스크립트 내부 로직까지는 안 봤기 때문이다.**

---

## 2026-08-26 (14차) — 전수 정합성 감사 (하루에 바꾼 게 너무 많았다)

하루에 1군/2군 폐지·맵 게이트 해제·동종업계 교체·맵 구조 수정·지지선 재설계까지 했다.
**변경 하나하나는 검증했지만 서로의 정합성은 안 봤다.** 사용자가 전수조사를 요청해서 돌렸고,
**침묵으로 깨질 문제 5건**이 나왔다. 전부 에러가 안 나고 결과만 틀리는 종류다.

### 찾은 것 5건

**① 백테스트 표가 09-01에 깨질 상태였다** ⚠️ 가장 심각
`weekly-action-review`에 `순위` 열을 추가하면서 표 헤더를 16열로 바꿨는데,
`backtest-track-record.md`의 실제 표는 **15열 46행**이었다. 09-01 주간리뷰가 16열 행을
append하면 **표가 깨지고 값이 한 칸씩 밀려 들어간다.**
→ 파일을 16열로 마이그레이션(백업 후). 과거 순위는 일일 로그로 복원 시도했으나
**43행 중 2행만 매칭**됐다(백테스트는 07-07부터, 일일 로그는 그 뒤부터). **없는 값을 지어내지 않고**
이 파일이 이미 쓰는 방식대로 `미확인(구버전)`으로 뒀다. 열수 검증 통과(16열 46행, 불일치 0).

**② 강화-TA가 아직 "예산 부족 시 생략" 목록에 있었다**
`compute_ta.py --codes`로 바뀌어 **MCP 0회**인데 생략 대상에 남아 있었다.
예산 때문에 생략할 이유가 없고, **생략하면 SMA·지지저항·52주고점을 잃어 등급조정④를 못 한다.**
같은 이유로 조건부-C·D2(⑬)도 생략 대상에서 뺐다.

**③ 후보 4개인데 하류가 3개·2개에 멈춰 있었다**
`entry-check` 카카오 "최대 3개", 브리핑 카카오 "그 밖의 후보 최대 2개".
후보 상한을 2→4로 올렸으므로 **4번째 픽이 조용히 빠진다** — 그 종목은 아무도 확인하지 않는다.
→ 각각 4개·3개로. entry-check 분량 기준도 "200자 안팎"→"픽당 2줄, 400자 이내"로 고치고
**"넘치면 근거를 줄이지 픽을 빼지 않는다"**를 명시했다.

**④ 주간리뷰가 옛 로그 형식만 알고 있었다**
STEP2의 파싱 명세가 `후보1·후보2·2군:` 그대로였다. **후보3·후보4를 못 읽는다.**
→ 형식 A(08-27 이후)·형식 B(이전)를 나란히 적고, **한 주에 두 형식이 섞이는 09-01 케이스**를
명시했다(08-24~26 형식 B, 08-27~28 형식 A).

**⑤ 폐지된 🟠가 entry-check에 남아 있었다** — 배지 목록 `🔴/🟢/🟡/🟠`.

### 덤으로 정리한 것

**복수 섹터 등재를 조용히 버리고 있었다.** 한화시스템·켄코아에어로스페이스가 방산·우주 양쪽에
등재돼 있는데(실제로 두 사업을 다 한다), `map_peers`가 **먼저 나온 섹터만 쓰고 나머지는 버렸다.**
→ `다른섹터_소속`으로 함께 돌려준다. 재료가 우주 쪽인데 방산으로 비교하면 해석이 틀어진다.

### 이상 없음을 확인한 것

- 스크립트 호출 계약 15건 — 인자 불일치 0
- JSONL 스키마 3자 정합(SKILL ↔ append_log ↔ check_entry) — 19필드 전부 일치
- `check_jargon.py` 출력 키 ↔ SKILL 참조 일치
- `check_entry.py`의 조건 판정은 **타입 무관**(현재가 vs 값 비교)이라 지지선 재설계와 충돌 없음
- `rank`는 그대로 통과만 시키므로 의미 변경의 영향 없음
- 런처 allowlist — 오늘 **새 도구 종류를 추가하지 않았다**(전부 PowerShell 경유). 수정 불필요
- 데이터 파일 3종 파싱 실패 0, BOM 없음
- 맵 파서 왕복: 14섹터 140종목, 빈 섹터 0

### ⚠️ 교훈

**변경 하나하나를 검증하는 것과 변경들이 서로 맞는지 보는 것은 다른 일이다.**
5건 전부 "그 변경만 보면 옳은데 다른 파일과 안 맞는" 종류였고, 전부 **에러 없이 결과만 틀린다.**
특히 ①은 다음 실행(09-01)에서야 드러났을 것이고, 그때는 표가 이미 오염된 뒤다.

→ **규칙: 한 세션에서 여러 파일에 걸친 변경을 했으면, 끝에 정합성 전수조사를 돌린다.**
   특히 ⓐ 파일 형식(열·필드)을 바꿨을 때 ⓑ 개수 상한을 바꿨을 때 ⓒ 개념을 폐지했을 때.

---

## 2026-08-26 (13차) — 맵 건강검진 자동화, 죽은 종목 4개 추가 발견

12차에서 죽은 종목(`에이치디현대미포`)을 **수동으로** 찾았다. 반기마다 사람이 기억해서
할 일이 아니라 자동화했고, 만들자마자 **4개를 더 찾았다.**

### 만든 것

**`check_map_health.py`** — 맵 전 종목의 마지막 거래일과 섹터 동조성을 잰다(132종목, **약 2초**).
`value-chain-map-updater`의 **STEP 0-3**(가장 먼저)으로 연결.

**`fetch_stock.py`에 매일 동작하는 필터** — 비교군 종목 중 거래가 멈춘 것을 그 자리에서 제외하고
`거래멈춤_제외`로 알린다. 맵 점검은 4·9월에만 도니 **그 사이에 죽는 종목은 매일 걸러야 한다.**

### 추가로 찾은 죽은 종목 4개

| 종목 | 섹터 | 상태 |
|---|---|---|
| 오스템임플란트 | 의료기기 | 가격데이터 없음(자진 상장폐지) |
| 씨아이에스 | 2차전지 소부장 | 가격데이터 없음 |
| 소프트캠프 | 사이버보안 | 가격데이터 없음 |
| 비올메디컬 | 의료기기 | 2025-12-09 이후 거래 없음 |

⚠️ **죽은 종목에 두 유형이 있다는 걸 놓쳤다.** ①옛 데이터가 남아 있는 것(미포) ②데이터가 아예
없는 것(오스템 등). 후자는 `IndexError`로 터져 **"조회 실패"에 묻혀** 있었다. 없어진 게 아니라
**안 보이게 된 것**이라 더 나빴다.

**그리고 죽은 종목은 자기 섹터만 망치는 게 아니었다.** 이력이 짧아 **공통 표본 구간을 94일로
끌어내려** 전체 상관과 기준선을 흔들고 있었다. 정리 후 n=120으로 고정되고 기준선이 0.428에서
안정됐다. 12차에서 잰 숫자들은 그래서 오염돼 있었다(판정 자체는 그대로).

| 섹터 | 12차(오염) | 13차(정리 후) |
|---|---|---|
| 기준선 | 0.343 | **0.428** |
| 조선 본선 | 0.640 | 0.691 |
| AI 소프트웨어 | 0.499 | 0.580 |
| 의료기기 | 0.277 | 0.393 — 여전히 기준선 미달, **표시 유지** |

최종: **14섹터 132종목, 죽은 종목 0건, 조회실패 0건.** 13개 기준선 상회, 1개 표시로 격리.

### ⚠️ 이번에 배운 것 — 진단 코드는 알려진 정답으로 돌려봐야 한다

**검출 로직이 두 번 조용히 실패했고, 두 번 다 "0건"으로 보였다.**

1. **첫 번째** — 뒤처짐을 "관측된 서로 다른 마지막거래일 중 몇 번째인가"로 셌다. 대부분 종목의
   날짜가 같으면 목록이 `['20251212','20260826']` 둘뿐이라 **8개월 차이가 "1칸"**으로 계산됐다.
   → 달력일 차이로 수정.
2. **두 번째** — 매일 필터가 30일 창으로 조회하니 죽은 종목은 0행이 돼 **필터에 닿기 전에
   "조회 실패"로 빠졌다.** → 실패한 종목만 넓은 창으로 재확인해 `거래멈춤`/`데이터없음`/`일시실패`로 분류.

둘 다 **"제거했던 죽은 종목을 도로 넣어 잡히는지" 테스트**를 돌려서 잡았다. 그 테스트가 없었으면
"죽은 종목 0건"을 **문제 없음으로 읽고** 넘어갔을 것이다.

→ **규칙: 진단·검출 코드는 반드시 알려진 양성 사례로 한 번 돌려본다. "0건"은 "문제 없음"이 아니라
"검출이 안 되고 있음"일 수 있다.**

### 내일 브리핑 영향

- `check_map_health.py`는 **09-07에만** 돈다 → 내일 영향 없음.
- `fetch_stock.py` 필터는 매일 돈다 → **영향 있음.** 실측 검증 완료(정상 맵에서 제외 0건,
  죽은 종목 주입 시 두 유형 모두 검출). 실패해도 비교군이 짧아질 뿐이다.

---

## 2026-08-26 (12차) — 가치사슬맵 섹터 구분 타당성 실측

11차에서 동종업계 비교를 맵 기반으로 바꾸면서, **맵의 섹터 구분 자체가 옳은지**가 갑자기
중요해졌다. 사용자가 그걸 물었고, 이제 측정할 수 있는 문제가 됐다.

### 방법

맵 148종목 중 코드가 잡히는 133개의 **최근 120거래일 일간수익률**을 받아 섹터 내 평균
쌍별 상관을 쟀다. **기준선 = 서로 다른 섹터 종목 무작위 2,771쌍의 평균 상관 = 0.343.**
이건 "그냥 시장이 같이 움직이는" 수준이고, 섹터가 이걸 못 넘으면 **그 섹터 이름은 정보가 없다.**

⚠️ 기준선을 먼저 잡은 게 핵심이다. 절대 상관값만 보면 0.33도 "꽤 높네"로 읽히지만,
무작위가 0.343이므로 **0.33은 사실 무작위보다 나쁜 값**이다.

### 결론 — 14섹터 구분 자체는 유효하다

- **섹터 간 상관 최고 0.550**(2차전지↔원전)이고, 전부 각자 섹터 내부보다 낮다.
  → **합쳐야 할 쌍 없음.** 구분이 과하지 않다.
- **반도체 25종목이 0.634(강함)** → 2026-08-25 내 3분할 제안 기각이 데이터로 확인됐다.
  사용자가 *"반도체는 다 연결돼 있어서 소재가 올라도 장비·부품도 오른다"*고 한 그대로다.
- 14개 중 11개가 기준선을 유의미하게 상회. 문제는 3개였다.

### 찾은 것 ① — 맵에 **죽은 종목**이 있었다

`에이치디현대미포(010620)`의 **마지막 거래일이 2025-12-12**다. 상장폐지/합병된 종목이
8개월간 맵에 남아 있었고, 자기 섹터와 상관이 **−0.169(음수)**로 나와 걸렸다.

**이것 하나 때문에 `조선 본선`이 0.371(무의미)로 찍혔다. 제거하니 0.640(강함).**

⚠️ **죽은 종목 하나가 섹터 판단 전체를 망가뜨린다.** 그리고 11차에서 만든 맵 기반 동종업계
비교가 이 종목을 그대로 비교군에 넣고 있었다 — 하루만 늦었어도 브리핑이 8개월 전 종가와
오늘 종가를 나란히 놓고 "섹터가 어떻다"를 썼을 것이다.
→ **정기 갱신에 "마지막 거래일 확인"을 넣어야 한다**(아래 남은 것).

### 찾은 것 ② — 사명 불일치 4건이 조용히 종목을 빼먹고 있었다

현대일렉트릭→**HD현대일렉트릭**, 휴니드→**휴니드테크놀러지스**, 파수→**파수AI**,
한글과컴퓨터→**한컴**. 넷 다 종목코드가 안 잡혀 **동종업계 비교에서 통째로 빠지고 있었다.**
에러가 아니라 조용한 누락이라 알아채기 어려웠다.

나머지 8건(파나시아·HD현대삼호·HD현대로보틱스·롯데바이오로직스·마이허브·SK쉴더스·
마인즈랩·업스테이지)은 **진짜 비상장**이다 — `register_watchlist`가 매일 실패하던 바로 그 8개.
뉴스검색엔 쓰이므로 **지우지 않고 표시만** 남겼다.

### 찾은 것 ③ — 두 섹터는 가치사슬이 아니라 업종 묶음이었다

**의료기기/디지털헬스 0.277** — 무작위(0.343)보다 **낮다.** 11종목 중 10개가 서로 이질적.
쪼개도 안 됐다: 의료AI 0.442 / 체외진단 0.279 / **의료기기·미용 0.159(더 나빠짐)**.
→ 조선 기자재는 같은 조선소에 납품하니 같이 움직이지만, 인바디(체성분계)와
오스템임플란트(임플란트)는 **아무 납품 관계가 없다.** 묶을 근거가 처음부터 없었다.
→ **사용자 선택: 유지 + `동조성 낮음` 표시.** 발굴에는 쓰되 **섹터 단위 추론에는 쓰지 않는다.**
  SKILL에도 "이 표시가 있으면 `💡 섹터 참고`·`⚡ 섹터 강세`를 붙이지 않는다"를 넣었다.

**AI 소프트웨어 0.332** — 이질 3종목 제거로 **0.499(강함)** 회복.
- **더존비즈온 0.055** — ERP 전문 기업이라 AI 흐름과 사실상 무관. **이름값으로 들어와 있었다.**
- 오브젠 0.239 / 씨이랩 0.269.
⚠️ 뺀 것은 "나쁜 회사"라는 뜻이 아니라 *이 섹터의 동조 흐름을 대표하지 않는다*는 뜻이다.
   10차 이후 맵 밖 종목도 후보가 될 수 있으므로 재료가 강하면 그대로 올라온다.

### 최종

| | 이전 | 이후 |
|---|---|---|
| 조선 본선 | 0.371 무의미 | **0.640 강함** |
| AI 소프트웨어 | 0.332 무의미 | **0.499 강함** |
| 의료기기/디지털헬스 | 0.277 무의미 | 0.277 — **플래그로 오용 차단** |
| 코드 미해결 | 12건 | **8건**(전부 비상장, 정상) |

14섹터 144종목. **13개가 기준선 상회, 1개는 표시로 격리.**

### 남은 것

- **정기 갱신(`value-chain-map-updater`)에 두 가지를 넣어야 한다**:
  ① 각 종목의 **마지막 거래일 확인** — 죽은 종목 자동 검출
  ② **섹터 동조성 재측정** — 오늘 쓴 방법을 재사용해 기준선 미달 섹터에 플래그 자동 부착
  지금은 내가 수동으로 했고, 반기마다 사람이 기억해서 할 일이 아니다.
- 동조성 "보통" 2개(전력 인프라 0.480 / 휴머노이드·로봇 0.481)는 그대로 뒀다.
  기준선은 넘지만 강하지 않다 — 표본이 더 쌓이면 재검토.

---

## 2026-08-26 (11차) — 동종업계 비교군을 네이버 업종 → 가치사슬맵 동일섹터로

### 발단

사용자가 성광벤드 얘기를 꺼내 확인하다가, 그 종목의 `동종업계`가
**두산에너빌리티·레인보우로보틱스·두산로보틱스**로 나오는 걸 봤다. 성광벤드는 **피팅 제조사**다.
네이버 `industryCompareInfo`가 **업종("기계") 분류**라서 생긴 일이고, **동종업계가 아니다.**

이 필드는 6차에서 내가 "섹터 전체가 오른 건지 이 종목만 오른 건지 판단하는 근거"로 쓰라고
SKILL에 박아둔 것이다. **비교군이 틀리면 그 판단이 통째로 틀어진다.**
6차에서는 "개장 전 0.00" 문제만 고치고 **비교군이 맞는지는 보지 않았다** — 값이 나오는 것과
값이 옳은 것은 다른 문제인데 앞의 것만 확인했다.

### 바꾼 것

`fetch_stock.py`가 **가치사슬맵의 같은 섹터** 종목을 비교군으로 쓴다.
맵은 **납품·수주 관계가 확인된** 묶음이라 "이 섹터가 오르는가"에 정확히 맞는 표본이다.

- `data\dart-corpcode.json`(코드↔이름) + `value-chain-map.md`(섹터별 OR-목록)를 읽어 형제 종목을 찾는다.
- 각 종목을 일봉으로 조회(스레드 8개, 4종목 **1초**). 등락률 내림차순 정렬.
- 반환에 **`기준` 필드**를 넣어 신뢰도를 명시: `가치사슬맵 동일섹터` / `네이버 업종분류(폴백)`.

**개편 전후 (성광벤드)**

| | 비교군 |
|---|---|
| 전 | 두산에너빌리티·레인보우로보틱스·HD건설기계·두산밥캣·두산로보틱스·로보티즈 |
| 후 | **조선 기자재 8개** — 태광 +9.84 · 한국카본 +2.81 · 한화엔진 +2.53 · 동성화인텍 +1.25 · HD현대마린엔진 +1.15 · 삼영엠텍 +0.31 · 세진중공업 −0.47 · SK오션플랜트 −0.60 |

후자에서는 "섹터 전반이 오르는 중이고 성광벤드(+13.17%)가 그중 최상단"이 바로 읽힌다.
전자로는 아무것도 읽을 수 없었다.

**부수 효과 — 시각 의존성이 사라졌다.** 일봉 종가 2개로 계산하므로 개장 전이면 자연히 전일
등락률이 되고 `기준일`이 어제로 온다. 6차에서 넣었던 "전부 0이면 폴백" 분기가 맵 경로에는
아예 필요 없어졌다(폴백 경로에만 남겼다).

### 폴백 경로는 남기되 경고를 강하게 달았다

10차에서 **맵 밖 종목도 후보가 될 수 있게** 했으므로 폴백이 필요하다. 그런데 실측해보니
**폴백이 왜 위험한지가 그대로 드러났다** — 아스플로(반도체 가스부품)와 링크솔루션(우주 부품)이
`기계` 업종으로 묶여 **완전히 동일한 비교군**을 받았다. 서로 아무 관계도 없는 두 종목이다.

→ `_주의` 문구에 이 실측 사례를 그대로 넣고, **"후보와 사업이 무관하면 섹터 전반 판단의 근거로
쓰지 않는다"**를 명시했다. 그리고 그런 종목은 `unmapped`로 흘러 다음 갱신에 맵으로 편입된다
— 8차·10차와 여기서 고리가 닫힌다.

⚠️ **교훈: 새 데이터를 붙일 때 "값이 나오는가"와 "값이 옳은가"를 따로 확인한다.**
6차에서 앞의 것만 보고 넘어갔고, 사용자가 다른 얘기를 하다가 우연히 걸렸다.

### 참고 — 성광벤드 실적 (백테스트 표본)

08-24 브리핑 발굴가 22,950원 → 08-26 11:30 현재 29,650원 = **+29.2%** (3거래일).
08-24 +5.45% · 08-25 +8.26% · 08-26 +13.17%, 거래량 평소 3.5배.
⚠️ 다만 **이 종목의 갭은 이미 닫혔다** — "재료가 아직 주가에 안 붙었다"가 후보 근거였는데
3일 만에 +29%면 붙은 것이다. 진입체크의 `시초가 갭 +5% → 철회검토` 규칙이 이런 경우를 위한 것이다.

---

## 2026-08-26 (10차) — 1군/2군 폐지, 가치사슬맵을 자격요건→색인으로

9차를 반영한 직후 사용자가 두 번 더 되물었고, **둘 다 내가 절반만 고친 상태**였다.

### ① "이제 2군이라는 개념 자체가 없는 거 아니야?"

9차에서 나는 *"1군/2군 구분 자체는 남는다 — 검증 깊이가 아니라 그날의 순위다"*라고 썼고,
같은 문단에 *"2군이 1군보다 높은 등급을 받는 일은 있을 수 있다"*고도 썼다.
사용자가 그 모순을 짚었다 — **"그건 애초에 2군 개념이 남아있다는 거 아니야?"**

맞다. 논리를 끝까지 밀지 않았다:
- 1군/2군은 원래 **호출 예산 배분**이었다(상위 2개만 정밀검증).
- 예산이 남아 전부 정밀검증하게 되자 남은 건 **"검증 전에 찍어둔 순서"**뿐.
- 그런데 검증이 끝나면 **원점수라는 진짜 순서**가 나온다.
- **추정 순서와 실제 순서를 둘 다 들고 있으면, 어긋나는 순간 라벨이 사실과 다르다.**
→ 추정 쪽을 버린다. **1군·2군·🟠(간이검증) 전부 폐지.**

**바뀐 구조**
- 후보는 한 계층. 검증 대상 **최대 4개**(계층이 아니라 **예산 상한**), 전원 동일 검증·동일 등급.
- "구체성 기준"은 **누구를 검증할까**를 고를 때만 쓴다. **최종 순서를 정하는 데 쓰지 않는다.**
- 최종 순서 = **원점수 내림차순**. 일일 로그 `후보1:`~`후보4:`, JSONL `rank` = 1·2·3·4.
  ⚠️ `rank`의 의미가 바뀌었다(예전 `1·2=1군, 0=2군`).
- **표시 분량은 계층이 아니라 등급이 정한다** — 🔴/🟢 전체 카드, 🟡 압축(진입조건은 반드시 포함).
  어차피 사용자가 볼 때 중요한 축은 등급이지 계층이 아니다.
- 예산 부족 시 **후보 개수를 줄인다**(4→3→2). 검증을 덜 한 채 등급을 주지 않는다.

⚠️ **연쇄 수정이 컸다** — `weekly-action-review`(마크다운 파싱), `backtest-track-record.md`(순위 열),
제목 형식, 카카오 형식, 생략 순서, 자체 검산 문구까지. **로그 형식이 바뀌면 파서가 조용히 깨진다**
(Cowork DECISIONS 96·122차의 재발 패턴). 다음 주간리뷰(09-01)가 다루는 08-24~28 주는
**두 형식이 섞이므로** 둘 다 처리하라고 명시했다.

### ② "가치사슬맵에 없더라도 신호가 강하면 후보로 올려야 해"

사용자가 맵의 **목적**을 다시 짚었다 — *"미장 뉴스에서 신호가 왔을 때 해당 섹터의 연관 종목을
빨리 캐치할 수 있게 만든 것"*. 즉 **색인**이다.

**그런데 SKILL은 그걸 화이트리스트로 쓰고 있었다.** 맵에 없으면 재료가 아무리 좋아도
"정식 후보 불가"로 버렸다. **색인을 자격 요건으로 쓰면 색인에 없는 것은 아예 안 보인다.**
2026-08-26 하루에만 아스플로(297억 공급계약)·링크솔루션(한화에어로 우주발사체 수주)이
그렇게 버려졌고, 해운은 5거래일 연속 최상위 신호였는데도 버려졌다.

**→ 맵 소속을 후보 자격 요건에서 제거했다.**

⚠️ 다만 **맵이 대신해주던 검증까지 버리지는 않았다.** 맵의 진짜 가치는 "이 종목만 허용한다"가
아니라 **"이 종목이 그 테마와 실제로 연결돼 있음을 이미 확인해뒀다"**는 것이다. 그래서:

| | 맵 안 | 맵 밖 |
|---|---|---|
| 연결고리 | 검증 완료 — 생략 | **직접 확인 필수** |
| 근거 수준 | — | **공시·기사 원문에 명시된 사실**(계약 상대방·품목·금액·매출 비중) |
| 나머지 검증 | 동일 | 동일 |

- **추론으로 연결하지 않는다.** "반도체 장비사니까 HBM 수혜일 것"은 근거가 아니다.
- 맵 밖 종목을 후보로 올리면 STEP8 `unmapped`에 **반드시** 넣는다 → 다음 갱신에 색인 추가.
- 일일 로그 후보 줄에 **`맵:등재/미등재`** 필드 신설.
- **맵은 계속 먼저 본다.** 자격 요건이 아니게 됐을 뿐 **발굴 속도의 핵심**이다.

⚠️ **교훈: 도구의 목적과 실제 사용처가 어긋났는지 주기적으로 본다.** 맵은 "빨리 찾기"로
만들어졌는데 "이것만 허용"으로 쓰이고 있었고, 그 어긋남이 **매일 후보를 버리고 있었다.**
8차에서 미매핑 집계를 만들며 "받아줄 쪽이 꺼져 있다"까지는 봤지만, **게이트 자체가 잘못이라는 건
못 봤다.** 사용자가 짚어줘서 알았다.

### 가치사슬맵 정기 갱신 실행 (사용자 승인)

`ValueChainMapUpdater`를 활성화(09-07 예정)하고 지금 1회 앞당겨 실행했다.
**7.5분 / 턴 39 / MCP 22회 / 출력 24,257토큰 ≈ $4.1.**

- **해운 신설(14번째 섹터)** — 팬오션·대한해운·HMM. 등장 5일·최장연속 5일로 기준 충족.
  ⚠️ 흥아해운은 **본사 이전 뉴스뿐이고 구체적 계약 근거가 없어 제외**했다. 기준 충족이
  자동 편입이 아니라는 규칙이 실제로 작동했다.
- 신규 종목: 현대힘스(조선기자재)·태웅(원전기자재)·디아이·유니테스트(반도체/HBM).
  디아이·유니테스트는 **08-26 브리핑이 "미등재"로 버렸던 바로 그 종목**이다.
- 재확인 7건, 삭제 0건, 보류 7건, 기준 미달 제안 7건.
- **신규 섹터 기준을 세웠다**: 등장 3일+ **AND 최장연속 3일+**, 1회 최대 1개, 종목 근거 2개+.
  ⚠️ 연속 조건을 따로 둔 이유 — 등장일수만 보면 **흩어진 단발성**이 통과한다.
  실측에서 `폭염 에어컨`이 등장 3일인데 최장연속 1일(07-30·08-06·08-14)로 걸러졌다.

---

## 2026-08-26 (9차) — ⑨ 2군 정식 등급 채택 (내 권고를 뒤집음)

### 내가 틀렸다

나는 ⓑ("2군을 1군 수준으로 검증하되 등급은 따로 집계")를 권했고, 근거로 **"백테스트 표본 연속성"**을 들었다.
사용자가 되물었다 — *"원래는 간이검증만 해서 표본에서 제외했는데, 이제 아니잖아?"*

확인해보니 **사용자가 맞았다.** 백테스트가 2군을 뺀 진짜 이유는 검증 깊이가 아니었다:

> `2군: 에스비비테크 | 🟠(간이검증) | … | 발굴시점가:**미확인**(강화-P 미실행)`

**발굴가가 없어서 수익률 계산 자체가 불가능**했던 것이다. 정식 검증하면 강화-P가 돌고 발굴가가 생긴다
→ 제외 사유가 그 순간 사라진다.

그리고 "따로 집계"는 **이미 공짜였다** — JSONL에 `rank` 필드(1·2=1군, 0=2군)가 처음부터 있었다.
별도 등급 체계를 만들 이유가 전혀 없었다.

⚠️ **내가 든 "표본 연속성"은 존재하지 않는 계열에 대한 걱정이었다.** rank=0 표본은 백테스트에
**한 건도 없었다.** 끊길 연속성이 애초에 없었고, rank 1·2 계열은 영향을 받지 않는다.
→ **교훈: "연속성이 깨진다"고 말하기 전에 그 계열에 표본이 실제로 있는지부터 센다.**

### 반영

- 2군에도 강화-F1·P·F2·VAL·RISK·TA·QUANT를 1군과 동일 적용. **정식 등급(🔴/🟢/🟡) 부여.**
- **`🟠(간이검증)` 등급 폐지**, `발굴시점가:미확인` 폐지, 2군에도 `entry_conditions` 기록
  → 09:05 진입체크 판정 대상이 된다.
- `weekly-action-review`: 2군 제외 규칙 해제. 단 **08-26 이전 2군은 그대로 제외** —
  그때 것들은 발굴가가 없어 계산 불가다. **없는 값을 추정해 채우지 않는다.**
- `backtest-track-record.md`: **`순위` 열 신설**(1군/2군). 등급을 나누는 대신 이 열 하나로
  "3·4순위 픽의 적중률이 다른가"를 답한다.
- 1군/2군 구분 자체는 **남는다.** 검증 깊이가 아니라 **그날의 순위**이기 때문이다
  (1군 = 구체성 기준 상위 2개).

⚠️ 2군이 1군보다 높은 등급을 받는 일이 생길 수 있다. **감추지 않고 그대로 쓴다** —
순위 선정 기준이 틀렸다는 신호이고, 그것도 `순위` 열로 측정된다.

⚠️ 예산이 부족한 날은 **2군 자체를 싣지 않는다.** 검증을 덜 한 채 등급만 주는 것이 가장 나쁘다.
MCP는 24회 → 약 32회 예상(상한 70).

---

## 2026-08-26 (8차) — ⑧ 미매핑 축적 구현

### 무엇이 문제였나

브리핑은 매일 "이건 가치사슬맵에 없어서 정식 후보로 못 올린다"는 것을 만난다. 그런데 그 사실이
**일일 로그에 산문으로만** 남아서 아무도 세지 않았다. 실측:

> `미매핑테마후보: 해운(… 08-19부터 7거래일 연속 미매핑 상태) / 인터넷·카탈로그소매(…)`

**같은 문장을 5일 내리 다시 쓰고 있었다.** 그 "연속"이야말로 맵에 넣으라는 신호인데 문장에 묻혀 있었다.
게다가 `value-chain-map-updater`의 STEP 1은 13섹터 **안에서** 종목을 찾는 구조라
**섹터 자체가 빠진 경우를 구조적으로 못 찾는다** — 해운이 매번 버려진 이유다.

### 어떻게 했나

- **새 파일 안 만들고 기존 경로에 얹었다.** 브리핑 payload에 `unmapped` 배열을 추가하면
  `append_log.py`가 `data\unmapped-log.jsonl`에 누적한다. **브리핑 쪽 추가 호출 0회**다
  (7차에서 "출력 토큰이 곧 시간"이라는 걸 확인한 직후라, 쓰기 단계를 늘리지 않는 쪽을 골랐다).
- `track_unmapped.py --report` — 등장일수·최장연속·현재연속·최초/최근·맵등재 여부를 계산.
- `--backfill` — 기존 마크다운 로그에서 1회 복원(19건, 07-29~08-26). `backfilled: true`로 표시.
  산문을 기계적으로 자른 것이라 정확하지 않을 수 있음을 데이터에 남겼다.
- **브리핑**: 웨이브A에서 `--report`를 읽어 "N거래일째"를 **추측이 아니라 계산으로** 쓴다.
- **value-chain-map-updater**: STEP 0-4로 STEP 1보다 **먼저** 읽는다. 섹터 누락을 여기서 잡는다.

첫 집계 결과 — **해운 등장 5일·최장연속 5일**, 폭염 에어컨 3일, 인터넷·카탈로그소매 2일.

⚠️ **판정은 넣지 않았다.** 스크립트는 세기만 한다. 등장 횟수는 **후보 자격이 아니라 우선순위**이고,
검증은 기존 기준 그대로 한다. 신규 섹터 신설은 13섹터 구조를 바꾸는 일이라 **사용자 확인 없이
하지 않는다**로 못 박았다(08-25 반도체 3분할 기각에서 배운 것 — 맵 구조는 함부로 건드리지 않는다).

### 만들면서 밟은 함정 2건 (둘 다 "조용히 틀리는" 종류)

**① 윈도 경로가 docstring에 들어가 파일 전체가 SyntaxError**
`data\unmapped-log.jsonl`의 `\u`가 유니코드 이스케이프로 해석됐다. 에러는 **2번째 줄**(docstring 시작)을
가리키는데 원인은 한참 아래라 찾기 어렵다. → 모듈 docstring을 `r"""`로 바꾸고 이유를 주석에 남겼다.

**② PowerShell이 붙인 BOM 때문에 JSONL 첫 줄이 조용히 사라졌다**
내가 파일을 `Set-Content`로 손댔더니 BOM이 붙었고, `load_rows()`가 `utf-8`로 읽어 첫 줄 파싱이
실패했다. **예외를 삼키는 코드라 에러도 안 났다.** 그 줄이 "없는 것"이 돼 백필 중복 검사를 통과했고
07-29 냉방주가 두 줄이 됐다. → 읽기를 전부 `utf-8-sig`로 바꿨다.
⚠️ **교훈: 이 프로젝트의 JSONL은 사람이 PowerShell로 손댈 수 있다. 읽기는 항상 `utf-8-sig`.**

### 남은 것

- 2026-08-26 브리핑 본문은 아스플로·링크솔루션·디아이·유니테스트를 "목록 미등재"로 언급했는데
  `미매핑테마후보` 줄에는 안 들어가 **집계에서 빠졌다.** 산문과 기록이 갈린 것이다.
  → SKILL에 "공시·뉴스에서 걸린 개별 종목도 `unmapped`에 넣는다"를 명시. 내일부터 잡힌다.
- 이름이 표기 흔들림으로 갈린다(`냉방주`·`폭염 에어컨`·`폭염 가전냉방`). 표본이 쌓이면 병합 검토.

---

## 2026-08-26 (7차) — 병렬화 기각, 지지선 재설계, 보류 항목 3건 해제

### 병렬화는 기각했다 — 시간이 거기 있지 않았다

사용자 요청("문제없으면 진행")이었지만 **먼저 재보니 할 이유가 없었다.**

| | 시간 |
|---|---|
| 스크립트 7종 순차 합계 | **24.0초** |
| 브리핑 전체 | 1,704초 |
| 비중 | **1.4%** |

전부 병렬로 돌려도 최상위 `fetch_us.py`(14.9초)로 수렴 — **최대 9초, 0.5% 단축**이다.

**시간이 어디로 가는지는 오늘 두 실행이 같은 답을 준다:**
- 브리핑: 출력 127,304토큰 / 1,704초 = **74.7 토큰/초**
- 진입체크: 출력 2,164토큰 / 32초 = **67.6 토큰/초**

→ **실행 시간 ≒ 출력 토큰 ÷ 70.** 스크립트가 아니라 모델이 글자를 쓰는 시간이다.
   **시간을 줄이려면 출력 토큰을 줄여야 한다.** 프로세스 병렬화로는 건드릴 수 없는 값이다.
   ⚠️ 앞으로 "느리다"는 문제를 만나면 병렬화·호출수부터 보지 말고 **출력 토큰부터 센다.**

단 하나 예외로 `fetch_us.py` 내부는 스레드로 묶었다. 야후 조회를 15개 → **25개로 늘리면서도**
16.2초 → 14.9초가 됐다. 순수 네트워크 대기 중첩이라 위험 없이 줄어드는 구간이었다.

### 지지선 재설계 — 20일 최저가는 지지선이 아니었다

6차에서 "쓸 수 없다"고 적어둔 문제를 여기서 고쳤다.

**무엇이 문제였나**: `support_20d` = 최근 20거래일 최저가. 2026-08-26 효성중공업에서
**1,834,000원(현재가 대비 -33%)**이 나왔다. 계산은 맞았다 — 시계열을 직접 확인하니 07-28~30에
실제로 -11%·-14%·-6% 급락했고 07-31에 +28% 반등했다. **분할·증자 왜곡이 아니라 진짜 급락**이었다.
문제는 계산이 아니라 **정의**다. 4주 전 패닉 저점은 사실이지만 오늘 손절선으로도 목표가로도 못 쓴다.

**어떻게 바꿨나**: 스윙 저점·고점(좌우 2봉보다 낮은/높은 자리)을 뽑아 가까운 것끼리 묶고,
현재가 기준 **바로 아래/위** 묶음을 고른다. 묶는 폭은 ATR에 비례(1~3%)시켰다 —
하루 9% 움직이는 종목과 3% 움직이는 종목에 같은 폭을 쓰면 한쪽은 다 흩어지고 한쪽은 다 뭉친다.

| | 기존(20일 최저/최고) | 신규(스윙 클러스터) |
|---|---|---|
| 효성중공업 지지 | 1,834,000 (**-33.4%**) | 2,640,000 (-4.6%) · 2회+ 2,485,000 (-10.2%) |
| LIG 지지 | 575,000 (**-16.9%**) | 674,000 (-2.5%) · **2회+ 656,000 (-5.1%, 터치 5회)** |
| LS ELECTRIC 지지 | 148,000 (**-24.8%**) | 194,400 (-1.0%, 터치 2회) |

- **`support`/`resistance`(최근접)와 `support_strong`/`resistance_strong`(2회 이상)을 따로 낸다.**
  효성중공업 최근접 저항이 **5개월 전에 한 번 스친 자리**(+0.4%, 터치 1회)로 나왔기 때문이다.
  사실이지만 그걸 목표가 근거로 삼을 수는 없다 — 모델이 고르게 둘 다 준다.
- `touches`·`last_touch`를 **반드시 함께 서술**하도록 SKILL에 못 박았다. 가격만 쓰면
  "5개월 전 1회"와 "최근 5회"가 똑같아 보인다.
- 10% 넘게 떨어져 있으면 `support_far: true` — **"가까운 지지 없음"은 그 자체로 판단 재료**다
  (손절 기준을 잡을 데가 없다는 뜻).
- 되돌림 계산은 **최근 120거래일만** 쓴다. 가격이력을 250일로 늘려도 1년 전 자리가 오늘
  지지선으로 올라오지 않게 하려는 것이다. → 항목 C와의 충돌을 여기서 해소했다.
- 구 필드는 `range_20d_low`/`range_20d_high`로 **이름을 바꿔 남겼다**(레인지로는 여전히 유효).
  `support_20d`·`resistance_20d`도 하위호환으로 남기되 **신규 서술 금지**로 표시.

### 보류 항목 해제 3건

**① 항목 C — 가격이력 100일 → 약 250거래일 (MCP −2회)**
`compute_ta.py --codes` 신설. **스크립트가 네이버 일별시세를 직접 받는다.**
- 이전 경로: API → 모델 컨텍스트 → **모델이 손으로 재출력** → 파일 → 스크립트.
  08-26 실행에서 모델이 써낸 `ta_in_0826.json`이 **12.9KB**였다. 모델이 숫자 배열의
  **복사 파이프** 노릇을 하고 있었고, 시간은 출력 토큰에 정비례하므로 그게 곧 시간이었다.
- 100일 상한은 **MCP 1회 요청 최대치**였을 뿐이다. 직접 부르니 268행이 온다.
  → **SMA120·SMA200** 신설.
- **부수 효과가 더 컸다 — 52주고점이 정확해진다.** 시세 API의 `week52_high`는 무상증자·액면분할
  미반영이라 2026-08-25 엘에스일렉트릭에서 907,000(비조정) vs 313,000(조정)으로 갈렸다.
  그날은 모델이 이상함을 알아채고 손으로 바로잡았지만, **못 알아채면 등급조정④가 잘못 걸린다.**
  이제 조정 시계열에서 직접 계산한다. → 강화-P의 `week52_high` 인계를 폐지.
- ⚠️ `--file`/stdin 폴백 경로는 그대로 뒀다(비상장·해외 종목, 네이버 차단 대비). 회귀 확인 완료.

**② 항목 B — MCP-GLOBAL을 뉴스검색 → 실제 시세 (MCP −4회)**
`fetch_us.py`에 `해외벤치마크` 블록 신설. TSMC `2330.TW` · CATL `300750.SZ` ·
라인메탈 `RHM.DE` · 중국선박 `600150.SS` 전부 야후에서 무료로 온다.
- **기각 이력이 뒤집힌 항목이다** — DECISIONS.md에 *"FMP 페이월로 대만·중국 개별 티커 대부분
  ACCESS DENIED, Exa 뉴스검색이 유일한 비페이월 경로"*로 남아 있었다. 페이월은 FMP의 것이지
  데이터의 것이 아니었다. **기각 사유를 "무엇이 막았나"까지 적어둔 덕에 되살릴 수 있었다.**
- ⚠️ 단서: **숫자는 뉴스보다 정확하지만 이유를 말해주지 않는다.** ±3% 넘게 움직였는데 설명이
  없으면 **"이유 미확인"으로 적게** SKILL에 규칙을 넣었다. 숫자만 보고 지어내면 더 나빠진다.
- `601989.SS`(중국중공업)는 없는 티커다(404). `600150.SS`를 쓴다.

**③ 미국 선물·원자재 신설**
`ES=F`·`NQ=F`·`CL=F`·`HG=F`·`GC=F`·`DX-Y.NYB`.
- **08:00 브리핑에 이게 왜 중요한가**: 미국 **현물장은 닫혀 있지만 선물은 열려 있다.**
  지수 종가는 어젯밤 이야기고 선물은 **지금 이 순간** 값이다 —
  "오늘 미국이 어떻게 열릴 것 같은가"를 말해주는 유일한 숫자이고 그게 곧 오늘 국장 방향이다.
- 2026-08-26 실측에서 **WTI가 -5.07%**였는데 그날 브리핑엔 통째로 빠져 있었다. 조선·정유·화학
  섹터 해석이 달라졌을 수 있는 크기다. 구리는 전력인프라·전선에 직접 붙는다.

### 누적 효과

MCP 호출: 30회 → **약 24회**(GLOBAL −4, TA 가격이력 −2).
모델 출력: `ta_in_*.json` 12.9KB 소멸(≈6K 토큰 ≈ **85초**).
데이터: SMA120/200·조정 52주고점·실제 해외 시세·선물·원자재·쓸 수 있는 지지저항 신설.

---

## 2026-08-26 (6차) — 첫 통합 실행 결과 점검

스크립트 8종 + JSONL 기록 + 09:05 진입체크가 **함께 돈 첫 날**. 둘 다 exit=0.

### 계측 — 비용이 84% 줄었다 (추정 아니라 실측)

| | 08-21 기준선 | 08-26 | |
|---|---|---|---|
| 턴 | 135 | **68** | −50% |
| 캐시읽기 | 30,210,387 | **8,056,024** | −73% |
| 캐시생성 | 4,908,584 | **319,100** | **−93%** |
| 출력 | 505,066 | **127,304** | −75% |
| 환산 비용 | $58.41 | **$9.20** | **−84%** |
| MCP 호출 | 48 | 30 | −38% |
| 소요 시간 | 27.7분 | 28.4분 | **거의 그대로** |

- **줄어든 건 토큰이지 시간이 아니다.** 턴이 절반인데 시간은 같다 = 턴당 시간이 늘었다.
  로컬 스크립트 8회가 턴을 잡아먹지 않고 데이터만 채워주기 때문이다. 시간을 더 줄이려면
  MCP 왕복이 아니라 **스크립트 병렬화**를 봐야 한다.
- 절감의 대부분은 **캐시생성 −93%**에서 나왔다. 거대한 MCP 응답이 컨텍스트에 얹힐 때마다
  캐시를 새로 쓰는 게 진짜 비용이었다. 호출 *횟수*가 아니라 **응답 크기**가 비용을 만든다.
  → 앞으로 최적화 우선순위는 "호출 줄이기"보다 **"큰 응답 걷어내기"**다.
- ⚠️ 이전에 세운 "$25~30" 추정은 **근거가 내 모델 하나뿐**이었고 3배 빗나갔다. 계측 전 추정치를
  근거로 결정하지 않는다.

### 고친 것 3건

**① 동종업계 등락률이 08:00엔 전부 0.00이었다** (`fetch_stock.py`)
- 08-25에 "섹터 전체가 오른 건지 이 종목만 오른 건지 숫자로 판단"하려고 넣은 필드인데,
  `fluctuationsRatio`가 **당일 세션 기준**이라 개장 전에는 값이 있을 수 없다.
  **쓰라고 만들어놓고 정작 브리핑 시각엔 무용지물**이었다(실행로그 실패항목 ③).
- 전부 0이면 일봉 종가 2개로 전일 등락률을 직접 계산하는 폴백을 넣었다.
  08-26 09:45 검증: 폴백값이 장중 실시간값과 **소수점까지 일치**(+1.07/−0.14/−1.87/+1.09/+0.89/+1.55).
- 같은 함정을 `fetch_us.py`에서 이미 겪었다(`chartPreviousClose`). **제공되는 '등락' 필드가
  어느 구간 기준인지는 신뢰하지 않고 종가 시계열로 직접 계산한다**를 규칙으로 굳힌다.
- `threeMonthEarningRate`는 함께 넣으려다 뺐다 — 3종목·9개 항목 **전부 `'N/A'`**였다.
  빈 값을 넘기면 브리핑이 "N/A"를 정보처럼 서술할 위험만 있다.

**② 임시파일 이름 충돌 — 매일 나고 있었다** (`SKILL.md` 0-1a 신설)
- `Write` 도구는 **이번 세션에서 읽지 않은 기존 파일을 덮어쓰지 않는다**. 임시 파일 이름이
  고정(`briefing.html`·`subject.txt`·`kakao.txt`·`daylog.json`)이라 어제 실행분이 남아 매번 막힌다.
- **실행로그엔 1건으로 보고됐지만 실제로는 상시였다.** 임시 폴더에 `briefing_0825`·`briefing_0826`·
  `kakao_0825`·`kakao_0825b`·`subject_0826`·`ta_in_0825`·`ta_in_0826`·`mdlog_0826`… 모델이 **매일
  새 이름을 지어내며 우회한 흔적**이 그대로 쌓여 있었다. 조용히 턴을 먹고 있었다는 뜻이다.
- 파일마다 고치지 않고 **STEP 0에서 폴더를 비우는 쪽**을 택했다. 이름을 매번 바꾸면 다음 단계
  경로가 어긋나고, 파일이 늘 때마다 같은 버그가 새로 생긴다.
- ⚠️ `gmail_step6_upgrade_*` 플래그는 청소 대상에서 뺐다. 지우면 같은 초안에 HTML 업그레이드가
  두 번 나간다(Cowork DECISIONS 76·123·125·131차).

**③ `list_drafts` 응답 초과 — 2일 연속** (`SKILL.md` STEP6 검증)
- 무인자 호출은 초안 20건 전문을 반환한다. 08-25 96KB, 08-26 143KB로 **검증 단계가 이틀 연속
  실패**했다(이 세션에서 나도 똑같이 터뜨렸다).
- `query:"subject:모닝 브리핑 newer_than:1d", pageSize:1, view:"DRAFT_VIEW_FULL"`로 고정. 실측 통과.
- `get_draft(MINIMAL)`은 대안이 아니다 — **스키마 설명과 달리 `subject`를 반환하지 않는다**(실측).

### 오탐 1건 — 08-25 제목 불일치

브리핑이 "08-25 Gmail 제목과 일일 로그 후보2가 불일치"라고 보고했으나 **초안이 두 개였던 것**이다.

| 시각(KST) | 마커 | 후보2 |
|---|---|---|
| 08-25 08:23 | `[코드]` | HD현대중공업 ← 로그와 일치 |
| 08-25 08:31 | 없음 | HD현대마린엔진 ← **코워크판** |

- 전일픽 계산은 로그 기준으로 했으므로 **결과는 옳았다**. 판단 근거를 로그에 둔 설계가 맞았다.
- 08-26에는 코워크 초안이 없다 → **코워크는 정상적으로 꺼졌다.** 확인 완료.

### 정상 확인된 것

- 08:00 브리핑이 쓴 `entry_conditions`를 09:05 체크가 그대로 읽었다(**한 번도 검증 못 했던 의존관계**).
  효성중공업 20일선 2,698,050 → 현재가 2,759,000 충족 → `진입가능`. LIG 746,450 → 701,000 → `보류`.
- 코스피 등락률에 실제 값이 들어왔다(`+0.68% 6,742.74`). `market_get_index`가 4회 연속 0을
  반환하던 자리를 `fetch_market.py`가 대체한 것이 확인됐다.
- 카카오에 등급 배지(🟡)와 `※ 조건 충족 ≠ 매수 권유`가 함께 나갔다.
- 진입체크 자체 비용은 무시할 수준이다 — 32초 / 6턴 / 출력 2,164토큰.

### 미해결 — 다음에 볼 것

- **지지선이 진입 판단에 못 쓸 수준이다.** 효성중공업 지지선 1,834,000원은 현재가 대비 −33%,
  LIG 575,000원은 −20%. `compute_ta.py`가 100거래일 최저가를 그대로 쓰기 때문이다.
  "매수 타이밍 포착"이 목표라면 **최근 지지·저항**이라야 쓸모가 있다. 가격이력 200일 확대
  (보류 항목 C)는 이 문제를 **더 악화**시킨다 — 함께 설계해야 한다.
- 실행 시간(28.4분)은 안 줄었다. 스크립트 병렬화를 봐야 한다.

---

## 2026-08-25 (5차) — 반도체 3분할 기각, 경로 정합성 점검

### 기각: 반도체/HBM 소부장을 3개 섹터로 분할

내가 제안했다가 사용자 지적으로 철회. **이유 둘 다 내 계산 착오였다.**

1. **사용자 지적**: "반도체는 다 연결돼 있어서 소재가 오르면 장비·부품도 오른다."
   실측 확인 — 서브체인 3개(HBM기판·장비·소재) 대표 9종목의 최근 5거래일 등락:
   **3일이 9종목 전부 같은 방향**(D-0 전원 상승, D-2 전원 하락, D-3 8:1), 2일만 엇갈림.
   → 나눠도 **같은 신호를 3개 섹터가 중복으로 내는** 셈이다.
2. **로테이션 점유율이 오히려 는다**: 13섹터 중 1개(1/13) → 15섹터 중 3개(**3/15 = 1/5**).
   "다양성 규칙에서 반도체 편중을 줄이자"는 목적과 **정반대 결과**가 나온다.

→ **13섹터 구분 그대로 유지.** 반도체 편중(58회 중 20회)은 섹터를 나눠서 풀 문제가 아니라
   실제로 그 섹터에 재료가 많았던 결과일 수 있다. 오늘 켠 13섹터 매일 스캔으로 0회 섹터
   (2차전지·의료기기·AI소프트웨어)가 살아나는지 **먼저 관찰**한다.

### 경로 정합성 점검에서 1건 발견

`Templates\` → `data\` 일괄 치환 때 **`DECISIONS.md`만 폴더 루트에 있는 걸 놓쳐** 스킬 2개가
존재하지 않는 `data\DECISIONS.md`를 가리키고 있었다. 수정 후 스킬·런처·예약작업이 참조하는
모든 경로가 실재함을 확인.

### 파일 정리

`data\` 15개는 전부 실사용. 미참조 백업 2건(`briefing-daily-log.md.bak-before-merge`,
`backtest-track-record.md.bak`)만 삭제. ⚠️ `dart-corpcode.json`(272KB)은 7일 캐시라 지우면 안 된다.

**Cowork `Templates\`(109개·2.4MB)는 그대로 둔다** — 이력 원본이 거기 있고, 되살려 쓰면
"서로를 2차 실행으로 오인"하는 문제가 재발한다.

## 2026-08-25 (4차) — 실행 전 최종 점검에서 치명적 결함 1건 발견·수정

- **`entry-check` 첫 실전 실행이 실패했다.** `run-skill.ps1`의 `--allowedTools`에 `PowerShell`이
  빠져 있어 **권한 거부 13건**으로 `run-py.ps1`을 호출하지 못했다. 브리핑 런처에는 처음부터
  있었는데 공용 런처에만 없었다. 수정 후 재실행: **25초, 턴 6, 정상 판정**.
  - ⚠️ 아이러니: [[AGENDA.md]] 6-3에 "스킬에 도구를 추가하면 런처의 `$allowed`도 같이 고칠 것"이라고
    적어두고 그 함정에 그대로 빠졌다. **문서화만으로는 안 막힌다** — 실행 테스트가 잡았다.
  - 안전망은 작동했다: 실패했는데도 카카오로 "실행 불가" 안내가 나갔다(조용히 죽지 않음).
- **실행 시간 실측 25초** — 내가 추정했던 "5~7분"과 크게 달랐다. 09:05 실행 시 09:05:30에 끝나므로
  출근 전 행동 창(~09:30)에 25분 가까이 남는다.
- **`✅ 진입가능`에 등급 배지를 함께 표기하도록 수정.** 사용자가 "체크 표시는 진입해도 좋다는 거지?"라고
  물어 확인한 결함 — `✅`는 **"브리핑이 건 조건이 충족됐다"**일 뿐 매수 권유가 아닌데, 등급(🟢/🟡)과
  주의사항이 카카오에 안 보이면 그렇게 읽힌다. 08-25 엘에스일렉트릭이 실제 사례: 조건은 충족했으나
  본문엔 "PER 100.63배·PBR 13.81배로 둘 다 매우 높다" 경고가 있었다.
- **`append_log.py`에 저장 시 검증 추가.** 진입 조건 누락을 **09:05가 아니라 08:00에** 드러나게 한다.
  저장을 막지는 않는다 — 브리핑 본문은 이미 발송된 뒤라 기록까지 잃으면 손해가 더 크다.
- **`check_entry.py`: "조건을 안 쓴 것"과 "조건이 없는 것"을 구분.** 이전엔 둘 다 "진입가능"으로
  처리해, 브리핑이 조건 숫자를 빠뜨리면 **조건이 있는데도 진입 신호를 보내는** 오판이 가능했다.
  필드 자체가 없으면 `확인필요`로 판정을 포기하고 사람에게 넘긴다.
- **스크립트 8종 스모크 테스트: 전부 정상**(총 21초). 테스트 하네스에서 PowerShell `$args` 자동변수
  충돌 버그를 발견 — 그 탓에 `fetch_market.py`가 `--summary` 없이 38,000자를 뱉고 있었다.
- **SKILL.md 정합성 점검**: 웨이브 구성·예산 생략 순서에 이미 제거된 항목(MCP-6 register_watchlist,
  검색2c-A 등)이 지시로 남아 있어 정리. 실행을 헷갈리게 할 수 있었다.

## 2026-08-25 (3차) — 데이터 소스 직접 호출 전환 (MCP 45회 → 약 18회)

**배경**: Cowork에서 코드로 옮긴 것만으로는 토큰 절감이 0이었다(같은 모델이 같은 일을 함).
실제 이득은 "로컬이라 가능해진 것"에서 나온다.

- **MCP-10~15 → `fetch_market.py`** (6회 → 0). Exa를 프록시로 쓰던 것이 원인이었다.
  실측 대조: 국고채 10년이 **Exa 경유 08-18자 vs 직접 호출 당일 16:07 기준**. 외국인 순매수 랭킹도
  08-18자 → 당일. 경제지표 캘린더는 과거 항목만 오다가 예정 항목으로 바뀜. **일주일 낡은 값으로
  시장을 판단하고 있었다.** 응답도 34,000자 → 2,200자.
  - 부수 효과: `koreaStock-market_get_index`가 이른 시각에 `change`를 0으로 반환하는 문제(07:00·07:30·
    08:00에 걸쳐 **4회 연속 실측**)가 함께 해소됨.
- **조건부-C·D2 → `fetch_stock.py`** (2회 → 0). 08-25 로그에 "consensus URL만 CRAWL_LIVECRAWL_TIMEOUT
  실패"가 있었는데, **Exa가 87자짜리 응답에서 타임아웃**을 낸 것이었다.
  - 이후 `integration`·`trend` 엔드포인트로 강화: **수급 10거래일**(등급조정의 "외국인 5거래일 연속
    순매도"를 이제 추론이 아니라 **계산**으로 판정) + **동종업계 비교**(이전엔 없던 정보 — "섹터
    전체가 오른 건지 이 종목만 오른 건지"를 숫자로 구분).
- **MCP-2/2.5/3/4 + 검색2c-A + 검색2a → `fetch_us.py`** (4회 → 0). 전부 키 불필요 공개 소스
  (미 재무부 CSV · Yahoo Finance · NASDAQ). 같은 날 FMP·bigdata가 준 값을 **소수점까지 재현**
  (S&P500 7,652.86 −0.28%, 나스닥100 29,023.18 −0.97%, 섹터 11개 순위 일치).
  - ⚠️ 함정: Yahoo의 `chartPreviousClose`를 쓰면 **"5일 전 대비"**가 나온다(−0.28%가 −1.19%로).
    종가 시계열의 마지막 두 값으로 직접 계산해야 한다.
- **MCP-6 + 강화-F1/D1/OWNERSHIP → `fetch_dart.py`** (7회 → 0). DART OpenAPI 키 발급 후 전환.
  - "임원자사주 응답 **130,667자**로 토큰 상한 초과 → 미확인" 사고가 해소됨(후보 2개 합쳐 4,227자).
  - "register_watchlist **8종목 매일 등록 실패**"가 소멸(관심종목 등록 절차 자체가 없어짐).
  - ⚠️ 조용한 버그: 대량보유 직전비율이 계속 빈 값이었다. 필드명이 `bfore_stkrt`가 아니라
    `stkrt_irds`였는데 **에러가 아니라 `null`로만 나와** 알아채기 어려웠다.
- **13섹터 매일 전수 스캔 → `fetch_sector_news.py`** (요일 분산 폐지).
  - Cowork DECISIONS 129차: *"완전한 매일 커버리지는 슬롯 1개로는 구조적으로 불가능… 예산상 비현실적"*
    → **MCP 예산 제약이었으므로 로컬에선 소멸.**
  - ⚠️ 함정 둘: (1) 구글뉴스 RSS는 **관련도순**이라 최신 기사가 묻힌다 — `when:3d` 연산자 필수
    (3/13 → 12/13 섹터로 개선). (2) 단어 나열식 쿼리가 오매칭을 낸다("사이버보안 공급계약 수주"에
    두산에너빌리티 발전소 기사) — 구절 검색으로 고쳐 13/13 달성.

### 정정 — DART 벌크를 "발굴"에 쓰려던 판단은 틀렸다

가치사슬맵 갱신에 DART 3개월 전수 조회(12,830건)를 쓰자고 제안했으나, 사용자가 "종목수가 너무
많아지는 것 아니냐"고 지적해 실측했다. **1개월에 고유 종목 212개, 그중 맵에 없는 것이 187개**
(맵 전체가 142개인데!). 상위는 코오롱·HDC·대우건설·금호건설 등 **거의 전부 건설사**였다.

원인: **방향이 반대였다.**
- 섹터 뉴스 검색 = 섹터 → 종목. 섹터 관련성이 **구조적으로 보장**된다.
- DART 벌크 = 공시 → 종목. **섹터 정보가 없어** 13섹터와 무관한 종목이 밀려든다.

→ **DART 벌크는 발굴이 아니라 검증(재확인·삭제)에 쓴다.** 맵의 142종목 중 25개(18%)가 최근 한 달
계약 이력이 확인됐는데, 이건 updater가 *"애매하면 손대지 않는다"*로 사실상 포기한 영역이다.

## 2026-08-25 (2차) — Cowork와 데이터 폴더 분리

- **사고**: 이식할 때 SKILL.md에 적힌 경로(`Claude\Templates`)를 그대로 썼는데 **그게 Cowork가 지금도
  쓰는 폴더**였다. 두 시스템이 같은 `briefing-daily-log.md`에 쓰면서 서로를 자기 재실행으로 오인했다.
  Cowork 로그에 그대로 남아 있다: *"1차 자동실행(성광벤드/두산에너빌리티 픽)이 이미 완료·로그 기록 후
  2차로 재실행된 것으로 추정"*, *"1차 실행 45회 포함 총 92/71회 초과"*. 코드판도 반대로 Cowork 항목을
  "08-24 2차 실행분"으로 기록했다 — **착각이 양방향**이었다.
- **캐시 교차 오염**: 코드판이 `캐시 08-18 재사용`(Cowork가 쓴 값)으로 계산 → A/B 비교가 같은 조건이
  아니게 됐다. 08-21에 경고했던 시나리오가 실제로 일어났다.
- **조치**: 데이터·스크립트·런처·로그를 전부 `morning breifing_code\`로 옮기고 Templates는 손대지 않음.
  Gmail draft ID로 코드판 항목만 골라내 이관(Cowork 항목이 코드판 draft ID를 *언급*해서 한 번 오판했다).
- **결정**: 전환 시 **Cowork 이력을 병합하지 않는다.** 판정 기준이 다른 두 시스템의 픽을 한
  트랙레코드로 섞으면 "갭 조합별 적중률" 학습 신호가 잡음이 된다.

## 2026-08-25 (1차) — 실행 시각 08:30 → 08:00, 계측 추가, 호출 상한 원복

- **08:00 이동**: 브리핑이 24분 걸리는데 08:30 시작이면 08:54 완료 → **개장 6분 전**이라 읽고 판단할
  시간이 없었다. 08:00이면 08:24 완료로 장전 동시호가 직전에 손에 들어온다.
- **호출 상한 70→95 완화를 원복.** 이식 때 "Cowork 제약이니 로컬에선 완화해도 된다"고 올렸으나
  **틀린 판단이었다.** 실측: MCP 48회에 모델 턴 135회, 입력 누적 35.1M(캐시읽기 30.2M + 생성 4.9M),
  Opus 5 환산 약 $58. **비용이 호출 수에 비례하지 않고 제곱에 가깝다**(매 턴 전체 컨텍스트 재전송).
  호출을 늘리는 대신 **응답 크기를 줄이는 쪽**이 옳다.
- **사용량 계측 추가**(`--output-format json`). 그전까지는 세션 기록을 손으로 파싱해야만 알 수 있었다.

## 2026-08-21 — Cowork → Claude Code 이식

- 스킬 3종(`morning-sector-briefing`·`weekly-action-review`·`value-chain-map-updater`) 이식.
  **판정 규칙·점수표·서술 원칙은 전부 원본 그대로.** 원본 섹션 중 빠진 것 0개.
- **환경 적응만 변경**: STEP 0 신설(도구 스키마 선조회 + 도구 이름 매핑), bash → PowerShell,
  긴 문자열은 파일 경유(콘솔 코드페이지 문제), Python 3.13 설치.
- ⚠️ **함정 3건 실측**:
  1. **`ToolSearch` 없이는 커넥터가 붙어 있어도 "도구 없음"**으로 실패한다(MCP 도구가 전부 deferred).
  2. Windows PowerShell 5.1은 **BOM 없는 UTF-8 스크립트를 ANSI로 읽는다** — 스크립트 안 한글이
     실행 시점에 깨져 **깨진 프롬프트가 claude에 전달**된다. 첫 테스트에서 실제로 겪었다.
  3. PATH의 `python.exe`가 Microsoft Store 스텁이라 종료코드 9009만 남긴다(겉보기엔 "있다"로 보임).
- **정정**: `seasonal-investment-calendar.md`를 "끊어진 참조"로 오판했다가 Cowork DECISIONS 60·88차에서
  *"어떤 SKILL.md도 읽지 않는 순수 브라우징용 참고자료"*로 의도된 설계임을 확인하고 되돌렸다.
  그대로 뒀으면 MCP-SEASON이 매번 통째로 생략됐을 것이다.

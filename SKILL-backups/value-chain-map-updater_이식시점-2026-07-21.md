---
name: value-chain-map-updater
description: 반기 가치사슬 맵 갱신 (4월/9월 첫 월요일) → value-chain-map.md 직접 업데이트
---

## 목적
매 4월/9월 첫 월요일, DART 연간·반기보고서 제출 완료 후 한국 가치사슬 맵을 갱신하고
`C:\Users\mrblue\Claude\Templates\value-chain-map.md` 파일을 직접(부분) 업데이트한다.

⚠️ **2026-07-14 전면 재작성 배경**: 이 SKILL.md의 STEP1(8섹터)·STEP3(이벤트체인 출력 형식)·STEP4(전체 덮어쓰기)가 실제 `value-chain-map.md` 파일의 진짜 구조(13섹터, OR-쿼리 백틱 문자열, morning-sector-briefing MCP-6·MCP-7이 그대로 파싱하는 포맷)와 완전히 어긋나 있었다 — SKILL.md가 갱신 안 된 채 파일만 수동으로 발전해온 것으로 추정. 2026-07-14 첫 실제 "지금 실행"에서 이 불일치가 발견돼(session이 그대로 덮어쓰면 5개 섹터·수십 개 종목·운영 메모가 통째로 사라짐을 감지하고 AskUserQuestion으로 사용자에게 직접 확인) 다행히 참사는 막았지만, **AskUserQuestion도 request_cowork_directory와 동일하게 무인 자동실행에서는 응답할 사람이 없어 행(hang)을 일으킬 수 있는 도구**라는 걸 새로 확인했다 — 9월 정식 무인 실행 때 똑같은 문제가 재발할 위험이 있었음. 이 문서는 그 교훈을 반영해 (1) STEP1을 실제 13섹터로 확장, (2) STEP3·4를 "전체 재생성"이 아니라 "실제 파일 형식(OR-쿼리)을 그대로 유지하며 섹터별 부분 수정"으로 재설계, (3) AskUserQuestion 의존을 완전히 제거(애매하면 그 섹터는 손대지 않고 넘어가는 안전 기본값 채택)했다. 이력: DECISIONS.md.

---

## ⚠️ 실행 규칙
1. **순차 실행. 병렬 금지.**
2. **MCP + 검색 합산 최대 26회.** (섹터 13개 × 2회, 2026-07-14 8섹터→13섹터 확장에 따라 16→26회 상향 — 반기 1회 실행이라 비용 부담 작음)
3. **web_fetch 사용 금지.**
4. **종목명 없는 카테고리 추가 금지.** 구체적 납품 관계 확인된 종목만 맵에 추가.
5. **AskUserQuestion 등 사용자 응답이 필요한 도구 사용 금지.** 이 태스크는 무인 자동실행이 기본이므로, 판단이 애매한 경우 반드시 "그 섹터/종목은 이번엔 손대지 않고 넘어간다"는 안전 기본값을 따른다 — 질문을 던지고 대기하지 않는다.

⚠️ **Templates 폴더 접근 (2026-07-21 신설, 명시적 마운트 호출 제거)**: 이 태스크는 `C:\Users\mrblue\Claude\Templates`에 직접 Write/Edit한다. 이전엔 STEP4 최초 동작으로 `request_cowork_directory` 명시 호출이 있었으나, 이 호출이 만드는 승인 팝업이 무인 자동실행 중 응답할 사람 없이 무기한 멈추는(hang) 위험이 있어 제거했다 — morning-sector-briefing에서 동일 조치로 hang이 해소된 것을 실측 확인했다(DECISIONS.md 61·63차). STEP4는 이제 별도 마운트 없이 바로 백업·Edit을 시도하며, 접근이 실제로 실패하면 STEP4의 위험 대응이 정상 에러로 처리해 STEP5에서 실패 알림을 보낸다 — 최악의 경우도 hang이 아니라 카카오 알림으로 끝난다. 이력: DECISIONS.md 64차.

---

## STEP 1: 섹터별 공급망 탐색 (순차, 섹터당 2회, 2026-07-14 13섹터로 확장)

각 섹터에 대해 아래 2회 호출 순차 실행:

**호출 A** — NaverSearch-search_news
```
쿼리: "[섹터 키워드] 납품 수주 계약 협력사 공급사 2025 OR 2026"
sort: "date"
display: 10
```

**호출 B** — easyGongsi-get_disclosures
```
stock: "[섹터 대표 소형주 또는 신규 발굴 종목]"
days: 180
```
→ 수주·납품 공시 확인. 계약 상대방 명시된 공시 우선.
→ ⚠️ **2026-07-03 확정**: 파라미터명은 `company`가 아니라 `stock`이다 — 실제 호출 테스트로 재확인(이전 값은 존재하지 않는 파라미터라 100% 실패).

**섹터 순서 및 키워드 (2026-07-14부터 13섹터, `value-chain-map.md`의 실제 `###` 헤딩과 1:1 대응)**:
1. 조선 기자재: "조선 기자재 부품 납품 수주 협력사"
2. 조선 본선: "조선 본선 수주 인도 협력사"
3. 방산: "방산 K방산 부품 납품 수주 협력사"
4. 원전 기자재: "원전 SMR 부품 설계 납품 수주"
5. 전력 인프라/변압기: "전력 인프라 변압기 수주 납품 데이터센터"
6. 2차전지 소부장: "2차전지 소재 부품 납품 수주 협력사"
7. 휴머노이드/로봇: "로봇 감속기 모터 액추에이터 납품"
8. 바이오 CDMO: "CDMO 바이오 위탁생산 계약 납품"
9. 의료기기/디지털헬스: "의료기기 디지털헬스 인허가 공급계약"
10. 사이버보안: "사이버보안 보안 납품 계약 공공"
11. AI 소프트웨어: "AI 소프트웨어 공급계약 수주"
12. 우주/스페이스X: "우주 위성 발사체 부품 납품 수주"
13. 반도체/HBM 소부장: "HBM TC본더 소켓 기판 납품 수주"

---

## STEP 2: 변경사항 판단

각 섹터별로:
- **추가**: 새롭게 발굴된 종목 (납품 관계 공시/뉴스 확인된 것만, `value-chain-map.md`의 해당 섹터 OR-목록에 이미 있는 종목은 제외)
- **재확인**: 기존 종목 중 2025~2026년 공급계약 공시로 "여전히 유효한 관계"임이 재확인된 것 (목록 자체는 변경 없음, 변경이력에만 기록)
- **삭제**: 납품 관계 소멸 공시 또는 사업 철수가 명확히 확인된 것만 (애매하면 삭제하지 않음 — 기본값은 "유지")

**추가 조건 (둘 다 충족해야 함)**:
- NaverSearch 또는 easyGongsi에서 구체적 납품·수주 관계 확인
- 카테고리명만 있고 종목명 없는 경우 → 추가 금지

**판단이 애매한 경우 (2026-07-14 신설)**: 계약 상대방이 명시 안 된 공시, 신뢰도가 낮은 단편적 언급 등으로 추가·삭제·재확인 어느 쪽도 확신이 안 서면 → 그 종목/섹터는 이번엔 아무 것도 하지 않는다(파일도 안 건드리고 변경이력에도 안 남긴다). AskUserQuestion으로 묻지 않는다 — 다음 반기(4월 또는 9월)에 더 명확한 근거가 쌓이면 그때 다시 판단한다.

---

## STEP 3: 이번 갱신 내용 정리 (STEP 4에서 실제 파일에 반영할 내용)

⚠️ **2026-07-14 전면 재설계**: 기존엔 이 STEP이 "이벤트 중심 수직 체인" 형식으로 파일 전체를 새로 만드는 절차였으나, 실제 `value-chain-map.md`는 13개 섹터 각각이 `### 섹터명` 헤딩 + OR-쿼리 백틱 문자열 한 줄로 구성돼 있고(예: `` `"HD현대마린엔진 OR 세진중공업 OR ... [이슈 키워드]"` ``), 이 형식은 morning-sector-briefing의 MCP-6(`register_watchlist`)·MCP-7이 그대로 파싱하는 데 쓰인다 — 다른 형식으로 덮어쓰면 그 파싱이 깨진다. 이 STEP은 그 실제 형식에 맞춰 "무엇을 바꿀지"만 정리하고, STEP 4에서 해당 섹터 줄만 부분 수정한다.

⚠️ **서브체인 섹터 예외 (2026-07-16 신설)**: 반도체/HBM 소부장 섹터는 2026-07-16부터 단일 OR-쿼리 줄이 아니라 서브체인 3개(HBM 기판/패키징·반도체 장비·반도체 소재/부품) + "전체" 블록, 총 4개 줄로 구성돼 있다. 이 섹터를 다룰 때는 아래 STEP4의 서브체인 예외 절차를 참고.

각 섹터에 대해 아래 중 하나로 결과를 정리:
- **변경 없음**: STEP 2에서 추가·삭제·재확인 어느 것도 해당 없음 → STEP 4에서 이 섹터는 건드리지 않는다.
- **종목 추가**: 해당 섹터의 OR-쿼리 문자열 끝, ` [이슈 키워드]` 바로 앞에 ` OR [신규종목명]`을 삽입할 위치와 종목명을 정리. (반도체/HBM 소부장이면 어느 서브체인 블록 + "전체" 블록에 넣을지도 함께 정리 — 아래 STEP4 참고.)
- **종목 삭제**: 해당 섹터의 OR-쿼리 문자열에서 제거할 `[종목명] OR ` (또는 ` OR [종목명]`) 위치를 정리.
- **재확인만(목록 변경 없음)**: 변경이력에 기록할 종목명·근거 공시(날짜·유형)만 정리 — OR-쿼리 문자열 자체는 그대로.

변경이력 문단은 기존 파일 상단(2026-07-10·2026-07-14 항목 등)과 동일한 스타일로 작성한다:
```
⚠️ **가치사슬 맵 갱신 이력 ([YYYY-MM-DD], 정기 자동실행)**: DART [연간/반기]보고서 기준 13개 섹터 리서치 수행. 신규 추가: [섹터](종목명 — 근거 공시 요약). 기존 종목 재확인(목록 변경 없음): [종목명(섹터) — 근거], ... 삭제: [종목명(섹터) — 사유] 또는 "없음". 백업: `Templates\SKILL-backups\value-chain-map_[YYYY-MM-DD].md`. 이력: DECISIONS.md.
```

---

## STEP 4: value-chain-map.md 파일 부분 갱신 (전체 덮어쓰기 금지)

⚠️ **(2026-07-03 결정, 2026-07-14 절차 전면 수정)** morning-sector-briefing SKILL.md는 가치사슬 맵을 인라인으로 담지 않는다 — MCP-7·MCP-6 단계에서 `Read("C:\Users\mrblue\Claude\Templates\value-chain-map.md")`로 이 파일을 그대로 참조한다. 이 STEP은 `update_scheduled_task`로 morning-sector-briefing을 건드리지 않는다 — `value-chain-map.md` 파일 자체만 갱신하면 다음 실행부터 자동 반영된다.

**절차:**
1. 덮어쓰기 전, 기존 `value-chain-map.md` 내용을 `C:\Users\mrblue\Claude\Templates\SKILL-backups\value-chain-map_[YYYY-MM-DD].md`로 스냅샷 저장한다.
2. STEP 3에서 "종목 추가"·"종목 삭제"로 정리된 섹터만, `Edit` 도구로 **해당 섹터의 OR-쿼리 문자열 한 줄만** 정확히 찾아 수정한다. 다른 섹터·다른 줄은 절대 건드리지 않는다. **`Write`로 파일 전체를 재생성하지 않는다.**
   ⚠️ **서브체인 섹터 예외 (2026-07-16 신설)**: 반도체/HBM 소부장 섹터는 단일 OR-쿼리 줄이 아니라 서브체인별 블록(HBM 기판/패키징·반도체 장비·반도체 소재/부품) + "전체" 블록, 총 4개 줄로 구성돼 있다. 이 섹터에서 종목 추가·삭제 시: (a) 신규 종목이 어느 서브체인에 속하는지 사업내용 기준으로 판단해 그 서브체인 블록에 반영, (b) "전체" 블록에도 동일하게 반영(두 곳 다 수정 — 하나만 고치면 MCP-6·MCP-7 참조 결과가 서로 어긋난다). 서브체인 소속이 애매하면 STEP2의 "판단 애매하면 손대지 않는다" 원칙을 그대로 적용해 이 섹터는 이번엔 건드리지 않는다. 다른 12개 섹터는 기존처럼 단일 줄 수정.
3. STEP 3에서 정리한 변경이력 문단을 `Edit`으로 파일 상단(가장 최근 기존 이력 문단 바로 아래, 첫 `### ` 섹터 헤딩보다 위)에 삽입한다.
4. 변경 사항이 하나도 없었던 반기(모든 섹터가 "변경 없음")면, 변경이력 문단만 "이번 갱신에서 신규 추가·삭제 없음, 기존 목록 유효"로 남기고 섹터 줄은 아예 건드리지 않는다.
5. morning-sector-briefing SKILL.md 자체는 건드리지 않는다.

⚠️ **위험 대응**: 백업·Edit 중 어느 하나라도 "outside this session's connected folders" 등 폴더 접근 오류로 실패하면, 이 STEP을 중단하고 value-chain-map.md는 건드리지 않은 채 STEP 5로 넘어가 실패 알림을 대신 발송한다. 절대 임의로 다른 방식으로 파일을 추측 갱신하지 않는다.

---

## STEP 5: 카카오톡 완료 알림

`mcp__e9af4b58-b31e-4ac9-9766-531ebf80ff8a__KakaotalkChat-MemoChat` 사용:

**정상 완료 시:**
```
🗺️ 가치사슬 맵 갱신 완료 ([YYYY.MM.DD])
- DART [연간/반기]보고서 기준, 13개 섹터 점검
- 추가 종목: [섹터별 종목명 나열 또는 "없음"]
- 삭제 종목: [섹터별 종목명 나열 또는 "없음"]
- 재확인(목록 변경 없음): [종목 수]건
```

**STEP 4 실패 시:**
```
⚠️ 가치사슬 맵 갱신 실패 ([YYYY.MM.DD])
- Templates 폴더 접근 실패
```
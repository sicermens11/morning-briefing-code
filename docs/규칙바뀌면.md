# 규칙이 바뀌면 다시 만들 것

⚠️ **2026-09-08에 이 목록이 없어서 두 군데가 낡은 채로 있었다.**
124차(나눠팔기)를 09-07에 채택했는데:
- `data/rule-capital.json` 이 **채택 전 값**(연 25.8% · 낙폭 -9.0%)으로 남아 있었다
- `build_rule_cases.py` 가 **앞 몫만** 계산해 화면의 최근 10건이 전부 +14.7% 였다

사용자 질문 *「테스트 결과 반영한다고 하면 기록 잘 정리되어 있어?」* 로 찾았다.

## 차례대로 다시 만든다

| # | 무엇 | 어떻게 | 무엇이 바뀌나 |
|---|---|---|---|
| 1 | 후보 뽑기 | `scripts/record_pick.py` 안의 `_몫들`·`_갭문턱`·`_후보수`·`_최대매수`·`_비중` | 매일 아침 뽑는 종목과 목표가 |
| 2 | 실제 매수 기록 | `scripts/bought.py` 안의 `_몫들`·`_목표`·`_최대보유` | 목표가 안내와 D+N 알림 |
| 3 | 사례·승률 | `python scripts/build_rule_cases.py` | `data/rule-cases.json` → 퀀트 3장 「과거에 어땠나」 |
| 4 | 자본 시뮬 | 그 규칙으로 lab 을 돌려 `data/rule-capital.json` 갱신 | 계좌 낙폭·연평균 |
| 5 | 빈도 | `python scripts/how_often.py` | `data/rule-frequency.json` → 「며칠에 한 번」 |
| 6 | 화면 문구 | `scripts/build_site.py` · `scripts/build_rule_html.py` | 「어떻게 파나」 등 서술 |
| 7 | 게시 | `powershell scripts/publish_pages.ps1` | 웹 반영 (캐시 10분) |

## 어긋나면 잡히게 해뒀다
`scripts/selfcheck.py` 가 **코드의 목표값과 rule-cases.json 의 규칙 문구가
어긋나면** 경고한다. 하나만 고치고 나머지를 잊는 것을 막는다.

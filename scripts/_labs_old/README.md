# 안 쓰는 옛 시험 파일 (2026-09-11 에 옮김)

예약·`.ps1`·스킬 **어디서도 안 부르는** 시험 파일이다.
지우지 않은 이유: **시험 결과의 재현 근거**다. `data/_labs/*.txt` 의 숫자가 무엇으로 나왔는지 여기 있다.

다시 쓰려면 `scripts/` 로 도로 옮기면 된다.
무엇이 살아 있는지는 `python scripts/check_dead.py` 로 본다.

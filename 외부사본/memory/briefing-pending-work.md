---
name: briefing-pending-work
description: "브리핑 시스템에서 오래가는 사실 — 공개 링크, 열쇠 만료, 비밀 취급 규칙"
metadata: 
  node_type: memory
  type: project
  originSessionId: 746aeb44-9d8e-447e-af4a-718406fc9f8d
  modified: 2026-08-31T04:57:15.004Z
---

⚠️ **할 일은 여기 적지 않는다.** 할 일은 `AGENDA.md`에 있고, 그날 볼 것은 그 파일 맨 위
「📌 다음 아침 확인 목록」에 있다([[daily-date-opens-checklist]]).
2026-08-31에 이 파일이 "카카오 200자 미해결·호출 상한 원복·사용량 계측"을 대기 중으로
적어 두고 있었는데 **셋 다 이미 끝났거나 무의미해진 일**이었다.
**끝난 일을 기억에 남기면 다음에 그걸 사실로 읽고 되풀이한다.**

## 공개 링크

**https://sicermens11.github.io/morning-briefing/** (아이폰 홈 화면에 올려 둔 그 주소).
Artifact는 안 쓴다 — 공유 링크가 **공유 시점 버전으로 고정**돼 나가서, 매일 갱신해도
링크로 들어온 사람은 옛것을 봤다. 목적 자체가 성립하지 않았다.

게시 경로: `run-briefing.ps1` → `scripts\publish_pages.ps1`
→ (`build_site` → `check_copy` → `check_layout` → `publish_pages.py`)

⚠️ **웹은 관문이 셋이라 지메일보다 잘 막힌다.** 실측 2회 중 1회 막혔다(지메일은 4회 중 0회).
   안전장치를 붙일수록 이 확률이 올라간다 — 그래서 **"안 쓴 날"만 막고 "짧게 쓴 날"은 올린다.**

## 열쇠 만료 — 셋 다 경고가 붙어 있다

| 열쇠 | 만료 | 만료되면 |
|---|---|---|
| GitHub 토큰 | 2027-08-27 | 링크는 살아 있고 **갱신만 멈춘다**(그날로 얼어붙음) |
| KRX 인증키 | 2027-08-28 | 네이버 폴백이 사라진다 |
| NH PLUG 앱키 | 2027-08-28 | **포트폴리오 화면이 조용히 빈다** ("종목 없음"으로 멀쩡해 보임) |

갱신하면 각 스크립트의 `KEY_ISSUED`도 같이 고친다. 경고는 만료 30~60일 전부터 실행 때마다 찍힌다.

## 비밀 취급 — 실제 사고에서 나온 규칙

⚠️⚠️ **`data\secrets.json`을 직접 읽거나 오류를 그대로 찍지 않는다.**
2026-08-27에 파싱 오류 메시지가 **파일 내용을 통째로 뱉어** 키 두 개가 대화 기록에 남았다.
확인은 **`scripts\check_secrets.py`로만** 한다 — 키 이름과 길이만 찍는다.

⚠️ DART 키는 계정당 하나뿐이라 **자가 재발급이 안 된다**(약관 명시). 읽기 전용 공개 데이터라
최악이 하루 호출 한도 소진이므로, 노출됐어도 교체하지 않고 두기로 했다.

⚠️ 비밀을 늘리는 제안은 한 번 더 따진다. 2026-08-31에 "지메일 전문을 스크립트로 넣자"며
앱 비밀번호를 제안했다가 **약한 이유(비공개 사본)를 지키려고 메일함 열쇠를 디스크에 두는 것**임을
깨닫고 스스로 기각했다.

관련: [[briefing-port-layout]] · [[evidence-before-rules]]

**Gmail 커넥터 토큰도 만료된다** (2026-09-14 발견). 9/12·9/13·9/14 사흘 연속 `create_draft`가
「requires re-authorization」으로 실패했는데 **아무도 몰랐다** — 브리핑은 나머지를 다 하고 로그에만 적는다.
고치는 건 claude.ai 커넥터 설정에서 Gmail 재인증(비대화형 세션은 못 한다). FMP도 같은 상태.
안 보낸 본문은 `%TEMP%\morning-briefing\gmail_body_unsent_YYYYMMDD.txt`에 남는다.
⚠️ 조판 코드(build_site·build_cards·quant_cards)는 메일을 안 부른다 — 디자인 작업과 무관.

# 시험 순서 · 2026-09-11 저녁

> ⚠️⚠️ **남은 시험 목록은 여기 적지 않는다.**
> `MASTER-STATUS.md` 의 **§2 남은 시험** 하나만 본다.
>
> 왜: 9/10 까지 이 파일과 `MASTER-STATUS.md` 가 **같은 목록을 두 벌** 들고 있었다.
> 하나를 고치면 나머지가 조용히 낡는다 — 오늘 규칙에서 겪은 것과 **똑같은 문제**다
> (`AUDIT-규칙어긋남-2026-09-11.md`). 목록은 **한 곳**에만 둔다.

여기에는 **순서를 정하는 기준**만 적는다.

---

## 1. 사용자가 정한 우선순위

> 「브리핑 목적은 **상승 기회를 포착해서 수익을 내는 게 가장 중요**하고,
> 매도 타이밍과 자산 보유 현황도 중요하지만
> **그것도 때문에 상승 기회를 놓쳐서는 안돼.**」

```
① 매수 (기회를 늘리는 것)      ← 먼저
② 매도 (번 것을 지키는 것)      ← 매수가 끝난 뒤
③ 운영·기록                   ← 시험이 도는 동안 (CPU 를 안 쓴다)
```

> 「**테스트 하는건 허락 안 받아도 되지만, 멈추는 건 허락 받아야해!**」

⇒ 시작은 내가 정한다. **중단은 반드시 묻는다.**

---

## 2. 순서를 정하는 기준 (위에서부터)

| | 기준 | 왜 |
|---|---|---|
| 1 | **다른 시험의 토대가 되는 것** | 토대가 틀리면 그 위의 결과가 전부 헛것이다.<br>250차(자르는 순서)가 이런 것이다 |
| 2 | **기회의 크기** | 「후보 40개로 자를까」는 **기회 60% 차이**다 |
| 3 | **격자의 빈 칸** | 3중 교차는 조합 격자의 마지막 빈 칸이다 |
| 4 | **새로 생긴 재료** | 잰 적이 없으면 모른다 |
| 5 | 매도 | 매수가 끝난 뒤 |

---

## 3. 판정 기준 (**내 맘대로 바꾸지 않는다**)

```
❌ 승률·평균 수익으로 판정하지 않는다
✅ **끝 자산**과 **낙폭** 으로 판정한다   (「평균 수익은 돈이 아니다」)
✅ **제약 없는 판**을 같이 본다          (현금·거래대금 한도를 뺀 판)
✅ **4관문**을 다 지나야 반영한다
     A 앞뒤 분할 (앞뒤 **둘 다** 같은 방향)
     B 해마다 승패
     C 무작위 대조 200번
     D 오차 시뮬
```
⚠️ A 만 통과한 것을 「통과」라고 적지 않는다. 249차가 지금 그 상태다.

## 4. 시험을 돌릴 때

```
LAB_OUT      결과 파일 이름을 **반드시** 준다 (안 주면 193차 파일을 덮어쓴다)
PYTHONIOENCODING=utf-8   화면이 cp949 면 「—」 한 글자에 죽는다
                         (2026-09-11 에 Tee 에 방어를 넣었지만 붙여서 나쁠 것 없다)
램            파이썬 **하나만** 띄운다. 동시에 두 개면 램 지킴이가 죽인다
              (전에 네 번 죽었다 — 23.7·21.6·18.2·23.1GB)
결과          `data/_labs/` 에 남긴다. Temp 에 두지 않는다
```
